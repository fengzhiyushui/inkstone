# agent-runtime 可维护性：阶段 1–2 成果与阶段 3–4 方案

- 类型：后端 spec
- 日期：2026-08-09（状态更新 2026-09-15）
- 状态：阶段 1–2 已完成；阶段 3–4 延后大版本
- 关联：[审计台账 #10](2026-07-12-agent-findings-remediation.md) · [v1.6 计划](../../plans/backend/2026-08-09-v1.6-backlog-closure.md)

---

## 问题与目标

`agent-runtime.js` 约 1160 行，职责混合、依赖集中、四条审批恢复样板重复。硬约束是非破坏性重构：公开 API、事件发布顺序、错误语义、审批暂停–续跑时序全部不变。本片先交零风险阶段 1–2，把是否重构建立在行为数据上。

## 决策

| 决策点 | 选择 | 理由 |
|---|---|---|
| 先刻画再动手 | 阶段 1 只加测试，不改实现 | 锁行为，防重构漂移 |
| 定级 | 阶段 3–4 延后大版本 | 核心运行时重构风险大 |
| 验收口径 | 边界清晰度与测试独立性 | 不看行数下降 |

## 已完成

### 阶段 1：行为刻画测试

`tests/unit/core/runtime/agent-runtime-characterization.test.js`，7 条：

| 刻画点 | 锁定内容 |
|---|---|
| 完整回合 | `user:message → agent:turn_started → agent:step → model:* → verification:result → agent:final` |
| 普通工具审批 | `turn:paused` / `approval:resolved` / `turn:resumed` 位置 |
| 主验证器审批 | 先 `verification:result` 再 pause；续跑重跑验证器 |
| 修复期工具审批 | 续跑继续 repair 循环；耗尽 → `agent:error` |
| 修复期验证器审批 | 续跑后 `repair:result` → `agent:final` |
| 未知审批 | `code = "APPROVAL_NOT_FOUND"` |
| 暂停中再 send | `code = "AWAITING_APPROVAL"` |

变异验证已执行：改错误码或交换事件顺序会使用例变红，还原后恢复。`tool:call`/`tool:result` 由 executor 发布，不在该序列内。

### 阶段 2：职责边界

| 区域 | 职责 | 性质 |
|---|---|---|
| `send` / 快答 / 工具环 / `verifyAndMaybeRepair` | 回合编排 | 副作用主导 |
| `approve` + 两条 verifier resume | 四条恢复分支 | 最复杂编排 |
| `savePausedRecord` 等 | sidecar 持久化 | 副作用封装 |
| `verifierApprovalResumeState` 等 | resume_state / 参数映射 | 纯函数 |
| 顶层 helpers | 决策归一、预算快照 | 纯函数 |

公共样板：重建 pending → grant → `publishTurnResumed` → 执行差异点 operation → 再暂停则 `savePausedRecord` + 清理返回；完成则 clear + idle。

## 阶段 3–4 候选（未开工）

1. 抽纯函数族迁出（风险极低）。
2. `resume-strategies` 参数化四分支样板（风险中，刻画测试可拦）。
3. 生命周期状态机与阶段执行拆分（风险高，单独评估）。

硬约束逐条验收：公开 API、事件顺序（刻画 + 集成测试）、错误语义 code/message、暂停–续跑时序不变。

## 边界

补丁/小版本不做结构重构。若大版本立项，建议顺序仍是纯函数 → resume-strategies → 状态机。

## 验收

阶段 1–2：刻画测试全绿且变异可红；边界分析归档。阶段 3–4：以当时独立 plan 为准。
