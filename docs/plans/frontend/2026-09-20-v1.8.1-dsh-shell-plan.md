# v1.8.1 壳层重构(DSH 参照 · β 路径)实施方案

> 完成状态以 [CHANGELOG](../../CHANGELOG.md) 为准。
> **状态:待执行。** spec:[2026-09-20-v1.8.1-dsh-shell-design.md](../../specs/frontend/2026-09-20-v1.8.1-dsh-shell-design.md)(§4.0 总原则已定:**全量搬设计事实,零引用 DSH 代码,与现有前端结合适配**)。
> 版本:**v1.8.1**(维护者称呼;按规则应为 minor,若维护者改口则四处版本同步改 1.9.0,本文其余不变)。分支 `feat/v1.8`(小版本分支复用;v1.8.0 已合入 main)。每层一个完整提交,提交信息不带 `Co-Authored-By`。
> 基线:`main @ f68ea19`(v1.8.0)· **1107 单测**(2026-09-20 实测)。

**本文对 spec §8 未拍板项的默认处理**(维护者不另指示即按此执行;改口则只改对应层):Q1 版本号 v1.8.1;Q2 设置**改模态**;Q3 已定 C 变体;Q4 右栏首批**三个面板都做**;Q5 轨迹 tab **做壳不做数据**(占位 + 空态文案,v1.9 契约到位后接数据)。

**执行者须知:** 本文所有 `文件:行` 在 `main @ f68ea19` 核实,相对仓库根 `d:\person studio\deepseek code\`;DSH 参照行号在 `D:\dsh\deepseek-harness\packages\client\`(v0.1.6-alpha.2),**只读取数值,不复制文件、不 import**。行号漂移以内容锚点为准。严格 TDD:先写失败测试 → 跑到失败 → 最小实现 → 跑绿 → 提交。

**环境与流程约束**(v1.7.2 / v1.8.0 执行经验):
- 分支:`git checkout -b feat/v1.8 main`(若远端已删则本地新建);**不要** `git worktree add`。
- 推送:每层只 `git push origin feat/v1.8`;**不推 main、不打 tag**——由维护者做。
- 路径:CHANGELOG 在 `docs/CHANGELOG.md`;索引在 `docs/README.md`。
- 计数口径:「N 处」= 引用次数;「N 行」= 行号数。
- **设置页在 B4 之前仍是全窗**(无 Rail),从设置回其它视图先 `click(".settings-back")`;B4 后设置是模态,smoke 链随之重写。

---

## 全局约束

- **八目录锁**:`src/core src/deepseek src/tools src/edits src/sessions src/context src/security src/workspace` 一行不碰;每层收口 `git diff --stat main..HEAD -- <八目录>` 为空。
- **零 DSH 代码引用**(spec §4.0 纪律 1–5):`gui/package.json` 不得新增 `@deepseek-ai/*`;`gui/src` 不得 `import '@deepseek-ai/…'`;不整文件拷贝;不引入 `--dsw-*` / `--dsh-*` 变量名;文件头不标 DSH 来源。**B0 首个任务就加守卫测试**。
- **18 槽 `tokens.css` 契约不变**(TUI 生成器正则:恰好 10 个 `[theme="id"]` 块、12 槽 `#rrggbb`);DSH 派生量全部进 `theme.css` 的 `[theme]` 别名块与 `:root`。
- **v1.4 定稿守 ①②③④⑤⑥⑦**(状态并入 `.cz-meta` / 侧栏三段 / 指标 5 形态 / kernel 零改动 / lucide / zh-en / 分支);**推翻 ⑧**(设置全窗→模态,Q2)。
- 五项准入(每层缺一不过):① `npm test` 只增不减 ② `npm run check` ③ `cd gui && npm run build:renderer` ④ `DEEPSEEK_CODE_GUI_SMOKE=1 node --test tests/e2e/gui-smoke.test.js` 含 `GUI_SMOKE_READY` ⑤ 八目录 diff 为空。
- **CSS Modules**:B0 起新组件一律 `X.module.css`(Vite 零配置,`import css from './X.module.css'`);`shell.css` 逐层迁出,B4 删空。类名保留 Inkstone 语义(`rail / cz / ev / th / sn`),不用 DSH 的 `dsh-*`。
- 视觉规则:字号只用 `12 / 13 / 14 / 17 / 18`(DSH 阶梯)+ 既有 `--fs-11` 仅限胶囊;图标只用 `lucide-react` 12/14/16;零 emoji;文案进 `strings.js` zh/en;可点击一律 `<button type="button">`;动效只用三档时长 `.1s / .2s / .3s` + 一条 easing;`prefers-reduced-motion` 全关。

---

## 文件结构总览

| 层 | 新建 | 修改 | 删除 |
|---|---|---|---|
| **B0** 壳层骨架 | `gui/src/state/columns.js` · `gui/src/components/v4/AppFrame.jsx` + `AppFrame.module.css` · `gui/src/components/v4/DragHandle.jsx` · `tests/unit/gui/columns.test.js` · `tests/unit/gui/no-dsh-imports.test.js` | `gui/main.js`(窗口参数)· `gui/index.html`(首帧着色)· `gui/src/App.jsx` · `gui/src/state/workbench-state.js` · `gui/kernel-host.js`(偏好三键)· `gui/src/styles/theme.css`(`--titlebar`)· `tests/unit/gui/dom-contract.test.js` · `tests/unit/gui/kernel-host.test.js` · `tests/unit/gui/workbench-state.test.js` | `gui/src/components/TitleBar.jsx` |
| **B1** token 重铺 | `tests/unit/gui/theme-structure.test.js` | `gui/src/styles/tokens.css`(重写)· `theme.css`(别名块重做 + 派生量)· `gui/src/state/themes.js` · `scripts/check-theme-contrast.mjs` · `scripts/gen-tui-theme.js`(不改契约,只改注释)· `src/apps/tui/theme-palette.js`(重生成)· `tests/unit/gui/theme-contrast.test.js` · `tests/unit/gui/theme-pins.test.js` · `strings.js` | — |
| **B2** 侧栏 + 会话页 | `Rail.module.css` · `ChatView.module.css` · `Composer.module.css` · `gui/src/components/v4/SessionHeader.jsx` + `.module.css` · `gui/src/styles/scrollbar.css` | `Rail.jsx` · `ChatView.jsx` · `Composer.jsx` · `MetricsLine.jsx`(只换类名)· `HomeView.jsx` · `strings.js` · `shell.css`(迁出对应段) | — |
| **B3** 右栏 dock | `gui/src/components/v4/Dock.jsx` + `Dock.module.css` · `gui/src/components/v4/panels/FilesPanel.jsx` · `ChangesPanel.jsx` · `RecoveryPanel.jsx` · `gui/src/state/file-tree.js`(扩:`treeFromPaths`)· `tests/unit/gui/file-tree.test.js`(扩)· `tests/unit/gui/dock-state.test.js` | `SecondaryViews.jsx`(ChangesView / RecoveryView 迁出为面板)· `App.jsx` · `workbench-state.js` · `strings.js` | — |
| **B4** 设置模态 + 收口 | `gui/src/components/Settings/SettingsModal.jsx` + `.module.css` · `docs/prototypes/v1.8.1-dsh-shell/screenshots/*.png` | `Settings.jsx`(改为模态内容)· `App.jsx` · `gui/main.js`(smoke 链)· `tests/unit/gui/dom-contract.test.js`(重写)· `tests/e2e/gui-smoke.test.js` · 版本四处 · CHANGELOG · docs/README · project-overview · v1.4 spec 附注 · 本文 | `gui/src/styles/shell.css`(删空)· `gui/src/components/Settings/ThemeHub.jsx`(并入 Appearance) |

---

# B0 · 壳层骨架

**目标:** 三列可拖拽 grid + 右栏轨道 + 40px 原生标题栏 + 首帧防闪。**完成后布局变了、皮肤没变**(仍是 v1.8.0 的 token)。

### 接口(B1–B4 依赖)

- `gui/src/state/columns.js` 导出:常量 `CENTER_MIN=400 · SIDEBAR_MIN=264 · SIDEBAR_MAX=420 · SIDEBAR_DEFAULT=280 · SIDEBAR_COLLAPSED=56 · SIDEBAR_AUTO_COLLAPSE=1024 · RIGHTBAR_MIN=300 · RIGHTBAR_MAX_RATIO=0.7 · RIGHTBAR_DEFAULT_RATIO=0.45`;函数 `clampWidth(px,min,max)`、`computeColumns(viewport, sidebar, rightbar, collapsedWidth=SIDEBAR_COLLAPSED) → { sidebar, center, rightbar }`
- `workbench-state.js` 状态新增 `sidebarWidth`(默认 280)、`rightbarWidth`(默认 0 = 关)、`rightbarOpen`(默认 false)、`dockTab`(默认 `"files"`);action `sidebar_resized {width}` · `rightbar_resized {width}` · `rightbar_toggled` · `dock_tab_changed {tab}`;既有 `railCollapsed` 语义不变(true → 侧栏 56px 轨)
- `kernel-host.js` 偏好白名单新增 `sidebarWidth`(264–420 整数,否则 280)、`rightbarWidth`(≥300 整数或 0)、`rightbarOpen`(boolean)、`dockTab`(∈ files/changes/recovery)

### B0.1 零 DSH 引用守卫(先于一切)

- [ ] **写测试** `tests/unit/gui/no-dsh-imports.test.js`:

```js
import test from "node:test";
import assert from "node:assert/strict";
import { readFileSync, readdirSync, statSync } from "node:fs";
import { join } from "node:path";

function walk(dir, out = []) {
  for (const n of readdirSync(dir)) {
    const p = join(dir, n);
    if (statSync(p).isDirectory()) { if (n !== "node_modules") walk(p, out); }
    else if (/\.(jsx?|css|mjs)$/.test(n)) out.push(p);
  }
  return out;
}
const ROOT = new URL("../../../", import.meta.url).pathname.replace(/^\/([A-Za-z]:)/, "$1");

test("gui never depends on or references the reference project's packages", () => {
  const pkg = JSON.parse(readFileSync(join(ROOT, "gui/package.json"), "utf8"));
  const deps = { ...pkg.dependencies, ...pkg.devDependencies, ...pkg.optionalDependencies };
  assert.deepEqual(Object.keys(deps).filter((k) => k.startsWith("@deepseek-ai/")), [], "gui/package.json 不得依赖 @deepseek-ai/*");
  for (const f of walk(join(ROOT, "gui/src"))) {
    const s = readFileSync(f, "utf8");
    assert.ok(!/from\s+['"]@deepseek-ai\//.test(s), `${f} 不得 import @deepseek-ai/*`);
    assert.ok(!/--dsw-|--dsh-/.test(s), `${f} 不得使用 --dsw-/--dsh- 变量名`);
  }
});
```

- [ ] 跑:应**直接绿**(现状无引用)——这条是护栏,不是 TDD 红绿;提交时带上。

### B0.2 列宽求解纯函数

- [ ] **写失败测试** `tests/unit/gui/columns.test.js`:

```js
import test from "node:test";
import assert from "node:assert/strict";
import { computeColumns, clampWidth, SIDEBAR_DEFAULT, SIDEBAR_COLLAPSED, RIGHTBAR_MIN, CENTER_MIN } from "../../../gui/src/state/columns.js";

test("clampWidth rounds and clamps", () => {
  assert.equal(clampWidth(263.6, 264, 420), 264);
  assert.equal(clampWidth(999, 264, 420), 420);
  assert.equal(clampWidth(300.4, 264, 420), 300);
});
test("sidebar 0 means collapsed rail; otherwise clamped", () => {
  assert.equal(computeColumns(1280, 0, 0).sidebar, SIDEBAR_COLLAPSED);
  assert.equal(computeColumns(1280, 100, 0).sidebar, 264);
  assert.equal(computeColumns(1280, SIDEBAR_DEFAULT, 0).sidebar, 280);
});
test("right column drops its track before center goes below CENTER_MIN", () => {
  // 1280 - 280 - 400 = 600 available → rightbar 350 fits
  assert.deepEqual(computeColumns(1280, 280, 350), { sidebar: 280, center: 650, rightbar: 350 });
  // 900 - 280 - 400 = 220 < RIGHTBAR_MIN → track removed, center takes all
  assert.deepEqual(computeColumns(900, 280, 350), { sidebar: 280, center: 620, rightbar: 0 });
});
test("rightbar clamps to 70% of viewport", () => {
  assert.equal(computeColumns(2000, 280, 5000).rightbar, Math.min(2000 - 280 - CENTER_MIN, 1400));
});
test("collapsedWidth 0 hides the sidebar column entirely", () => {
  assert.equal(computeColumns(1280, 0, 0, 0).sidebar, 0);
});
```

- [ ] 跑到失败(`ERR_MODULE_NOT_FOUND`)。
- [ ] **实现** `gui/src/state/columns.js`(原生 JS,不含 TS 类型;常量值来自 spec §4.0 表;算法:侧栏 0→轨宽否则 clamp;`available = viewport - s - CENTER_MIN`;右栏 0 或 available < RIGHTBAR_MIN → 0,否则 `min(available, clamp(rightbar, RIGHTBAR_MIN, viewport*0.7))`;center = `max(0, viewport - s - r)`)。
- [ ] 跑绿。

### B0.3 窗口与首帧

- [ ] `gui/main.js:67-74`(锚点 `new BrowserWindow({`):`width: smoke ? 1440 : 1280, height: smoke ? 900 : 840, minWidth: 880, minHeight: 600`;`titleBarStyle: "hidden"` 保留,**新增** `titleBarOverlay: { height: 40, color: <按偏好明暗>, symbolColor: <反色> }`——在 `host.init()` 之后、`loadFile` 之前读 `gui-preferences.json` 的 `theme` 经 `isLightTheme` 判明暗(`kernel-host` 暴露 `getPreferences()` 已有),明 `color:"#f9fafb", symbolColor:"#0f1115"`,暗 `color:"#1b1b1c", symbolColor:"#f9fafb"`;`backgroundColor` 同步为明 `#ffffff` / 暗 `#151517`(**解 v1.8.0 R10 首帧闪深色**)。
- [ ] `gui/index.html`:`<head>` 内联 `<style>html,body{background:var(--boot-bg,#151517)}</style>`,`<html>` 加 `data-boot-theme` 由 `main.js` 经 `loadFile(builtIndex, { query: { boot: light|dark } })` 传入,渲染层 `main.jsx` 第一行读 `location.search` 设 `documentElement.style.setProperty("--boot-bg", …)`。
- [ ] 窗口控件 IPC(`gui/main.js:219` 起的 `window:minimize/maximize/close`)与 preload 暴露**保留**——B4 前 `TitleBar.jsx` 仍在;B0 只把标题栏高度改 40 并让原生 overlay 接管窗控区域(`.titlebar .winctl` 隐藏,`-webkit-app-region: drag` 区域高 40)。

### B0.4 AppFrame + 拖拽

- [ ] **新建** `gui/src/components/v4/AppFrame.jsx`:三列 `grid-template-columns: ${sidebar}px ${center}px ${rightbar}px`(由 `computeColumns(viewportWidth, railCollapsed ? 0 : sidebarWidth, rightbarOpen ? rightbarWidth : 0)` 算得;`viewportWidth` 用 `ResizeObserver` 于 frame 根);children 三槽 `sidebar / main / rightbar`;`data-dragging` 在拖拽期间置位;`data-windows-titlebar` 恒置(Windows 唯一目标平台)。
- [ ] **新建** `DragHandle.jsx`:8px 命中带,`margin-left:-4px`,`cursor: col-resize`,`touch-action:none`,pointer capture;`onDrag` 回调 `(px)`;侧栏手柄 `left = sidebar`,右栏手柄 `left = viewport - rightbar`。拖侧栏 dispatch `sidebar_resized`,拖右栏 `rightbar_resized`;`onEnd` 时 `kernel.setPreferences({ sidebarWidth | rightbarWidth })`。
- [ ] **新建** `AppFrame.module.css`(数值来自 spec §4.0 壳层行):`.frame{display:grid;grid-template-rows:100%;height:100%;overflow:hidden;background:var(--bg-base);transition:grid-template-columns .3s var(--ease)}` · `.frame[data-dragging]{transition:none}` · `.frame[data-windows-titlebar]{padding-top:40px;background:var(--bg-panel)}` · `.frame[data-windows-titlebar] .center{background:var(--bg-base);border-radius:16px 0 0 0}` · `.frame[data-windows-titlebar]::before{content:"";position:absolute;inset:0 0 auto;height:40px;background:var(--bg-panel);-webkit-app-region:drag}` · `.sidebar{min-width:0;overflow:hidden;background:var(--bg-panel);border-right:.5px solid var(--border-strong)}` · `.center{min-width:0;display:flex;flex-direction:column;overflow:hidden}` · `.rightbar{position:relative;min-width:0;overflow:visible}` · `.handle{position:absolute;top:40px;bottom:0;width:8px;margin-left:-4px;cursor:col-resize;z-index:11;touch-action:none;transition:left .3s var(--ease)}` · `@media (prefers-reduced-motion: reduce){.frame,.handle{transition:none}}`。`--ease` 与 `--titlebar:40px` 在 B0 先加进 `theme.css:root`(`--ease: cubic-bezier(.4,0,.2,1)`)。
- [ ] `App.jsx`:`<div className="ide">` 内改为 `<AppFrame sidebar={<Rail…/>} main={<main className="pane">…</main>} rightbar={null /* B3 填 */} />`;保留 `.ide` / `.shell` / `.rail` / `.pane` 类名(smoke 门在 B4 前不动)。`theme.css:11` `--titlebar:30px` → `40px`。
- [ ] `workbench-state.js`:`createInitialState` 加四键;四个 action;`preferences_loaded` 透传(`sidebarWidth` 经 `clampWidth(…,264,420)`,`rightbarWidth` 非法回 0,`rightbarOpen` 布尔,`dockTab` 白名单);`kernel-host.js:89-100` 默认表 + `:151-168` normalize 同步。
- [ ] **改断言**:`tests/unit/gui/kernel-host.test.js` 两个 deepEqual 期望(锚点 `lastDark: "sumi"` 所在对象)各补 `sidebarWidth: 280, rightbarWidth: 0, rightbarOpen: false, dockTab: "files"`;`tests/unit/gui/dom-contract.test.js:16`(锚点 `--titlebar:30px`)→ `--titlebar:40px`;**加测试** `workbench-state.test.js` +2(四 action;`preferences_loaded` 非法值回默认)。
- [ ] 自绘 `TitleBar.jsx` 里的窗控三钮(`winctl`)本层**隐藏不删**(B4 删整个文件);语言/主题/菜单钮暂留。

### B0 收口

- [ ] 五项准入;预期测试 **1107 → ≈1115**(no-dsh 1 + columns 5 + reducer 2)。smoke 8 选择器原样。人工:拖侧栏 264–420、拖右栏(B0 右栏为空槽,`rightbarOpen` 先由一个临时 `Ctrl/⌘+J` 快捷键切换以便验证轨道)、窗口 <1024 侧栏自动收轨。
- [ ] **提交**:`feat(v1.8.1-b0): 壳层骨架 — 三列可拖拽 + 40px 原生标题栏 + 首帧防闪 + 零外部引用守卫`,推 `feat/v1.8`。

---

# B1 · token 重铺(C 变体)

**目标:** 结构槽走 DSH 明暗阶梯,10 主题只贡献 accent 与语义色;闸门改口径为 94 项常驻。**完成后界面骨架统一,10 套只差强调色。**

### B1.1 闸门口径先改(先测后码)

- [ ] `scripts/check-theme-contrast.mjs`(锚点 `export const GATE`):拆两组——`STRUCT_GATE`(7 对,对每个 `[theme]` 块算但**只按 group 去重**:`text/bg-base [7,19.5]`、`text/bg-panel [7,19.5]`、`text-mut/bg-base ≥4.5`、`text-faint/bg-base ≥3`、`border-strong/bg-base ≥1.5`、`text/bg-element [7,19.5]`、`text/code-bg [7,19.5]`)与 `ACCENT_GATE`(4 对,每主题:`accent/bg-base ≥3`、`accent-text/accent ≥4.5`、`ok/bg-base ≥3`、`err/bg-base ≥3`;`warn/info` 降为附加对不计入)。`checkThemes(css)` 返回 `rows` 含 `group` 字段;`:45` 的打印改 `${passed}/${total}`。
- [ ] `tests/unit/gui/theme-contrast.test.js:12-17`:`"all 10 themes pass all 11 gate pairs (110/110)"` → `"structure 2 groups × 7 + accent 10 × 4 = 54 rows all pass"`;`rows.length === 54`(结构 2×7=14 + 10×4=40)。**先跑到失败**(现行 tokens 是 110 行),再做 B1.2。
- [ ] **写测试** `tests/unit/gui/theme-structure.test.js`:解析 `tokens.css`,断言同 `group` 的所有主题 **10 个结构槽逐字节相等**(`bg-base bg-panel bg-element bg-inset text text-mut text-faint border border-strong code-bg`),且明组 `bg-base === "#ffffff"`、暗组 `bg-base === "#151517"`。跑到失败。

### B1.2 重写 `tokens.css`

- [ ] 文件头注释改写:「v1.8.1 · 结构槽 C 变体:明/暗两档中性蓝灰阶(参照值取自设计参照 `design-platform.css` 第 53–71 / 157–177 / 254–274 / 299–341 行);10 主题 id 保留,各自只定义 8 个强调/语义槽」。
- [ ] **结构槽实值**(两档,写死进每个块——不能用 CSS 变量引用,TUI 生成器要 `#rrggbb`):

| 槽 | 明(paper/latte/lotus/snow/sand) | 暗(sumi/slate/vesper/nord/ash) | 参照 |
|---|---|---|---|
| `bg-base` | `#ffffff` | `#151517` | bluish-00 / 950 |
| `bg-panel` | `#f9fafb` | `#1b1b1c` | sidebar-fill:50 / 900 |
| `bg-element` | `#ffffff` | `#2c2c2e` | input-major:00 / 850 |
| `bg-inset` | `#f9fafb` | `#1b1b1c` | code-block:50 / 900 |
| `text` | `#0f1115` | `#f9fafb` | label-primary:1000 / 50 |
| `text-mut` | `#61666b` | `#cfd3d6` | label-secondary:700 / 300 |
| `text-faint` | `#81858c` | `#adb2b8` | label-tertiary:600 / 400 |
| `border` | `#e1e5ee` | `#353638` | 200 / 800(alpha 描边的实色近似,alpha 版进 theme.css) |
| `border-strong` | `#cfd3d6` | `#616266` | 300 / 700 近似 |
| `code-bg` | `#f9fafb` | `#1b1b1c` | 同 inset |

- [ ] **强调槽**:10 主题各自保留 v1.8.0 的 `accent accent-hover accent-soft accent-text ok warn err info`;**但** `paper` 与 `sumi`(两档默认)的 `accent` 改为品牌蓝 `#4176e6`(deepseek-500)、`accent-hover #5686fe`(450)、`accent-soft` 明 `#edf3fe`(deepseek-50)/ 暗 `#2c2c2e`、`accent-text` 明 `#ffffff` / 暗 `#0f1115`;语义色两档统一 `ok #22c55e · warn #f59e0b · err #ef4444`,info 沿用各主题。其余 8 套 accent 不动(它们就是"配色")。
- [ ] 块顺序与 id 不变(`sumi slate vesper nord ash snow sand lotus latte paper`),`group` 不变;`tier` 保留。
- [ ] 跑:`node scripts/check-theme-contrast.mjs` **54/54**(若明组 `text-mut #61666b` on `#fff` = 5.9 ✓、`text-faint #81858c` = 3.6 ✓;暗组对称;accent 蓝 on 白 = 4.9 ✓ on `#151517` = 6.1 ✓ —— 执行者须亲眼看到);`theme-structure.test.js` 绿;`theme-pins.test.js` 原样绿(id 集不变)。
- [ ] `node scripts/gen-tui-theme.js` 重生成 `theme-palette.js`(生成物入库);`tests/unit/apps/tui-theme-palette.test.js` 原样绿。

### B1.3 `theme.css` 别名块 + 派生量

- [ ] `[theme] {…}`(`theme.css:15`)重做,**全部派生自 18 槽**:`--hover: color-mix(in srgb, var(--text) 8%, transparent)`(DSH hover alpha)· `--border-l1: color-mix(in srgb, var(--text) 4%, transparent)` · `--border-l2: … 10%` · `--border-l3: … 12%`(明)/ `[theme][data-dark]` 覆盖为 6/12/16%(暗)· `--shadow-1: 0 2px 4px rgba(0,0,0,.05)` · `--shadow-2: 0 4px 12px rgba(0,0,0,.02), 0 2px 8px rgba(0,0,0,.04)` · `--bubble: color-mix(in srgb, var(--accent) 10%, var(--bg-base))`(明)/ `var(--bg-element)`(暗)。
- [ ] `:root` 加:`--r-6 --r-8 --r-12 --r-20 --r-22 --r-pill:999px` · `--dur-fast:.1s --dur:.2s --dur-slow:.3s` · `--ease`(B0 已加)· `--lh-17:17px --lh-20:20px --lh-22:22px --lh-24:24px` · `--fs-17:17px`;既有 `--fs-11..18 --r-4..14 --sp-*` 保留。
- [ ] `themes.js`:`isLightTheme` 不变;新增 `isDarkTheme = !isLightTheme`;`App.jsx` 在 `setAttribute("theme")` 旁加 `documentElement.toggleAttribute("data-dark", !isLightTheme(state.theme))`。
- [ ] `strings.js`:`settings.appearance.sd` → 「明 / 暗两档 + 10 套配色 · 点击即刻预览」/ 英文对应;四档说明键保留(设置页仍按 tier 分组)。

### B1 收口

- [ ] 五项准入;预期 ≈1115 → ≈1117(structure +1,contrast 条目重写 ±0);`theme-hub.test.js` 原样绿。人工:切 10 套只见 accent 变、骨架不变;明暗切换整窗一致。
- [ ] **提交**:`feat(v1.8.1-b1): token 重铺 — 结构槽两档中性阶梯 + 10 套降为配色 + 闸门 54 项`。

---

# B2 · 侧栏 + 会话页

**目标:** DSH 的侧栏与会话页观感(数值全搬),Inkstone 的三段结构与卡片语义全保留。

### B2.1 滚动条 + 超椭圆(全局,零风险)

- [ ] **新建** `gui/src/styles/scrollbar.css`(参照 `scrollbar.css` 的双路径思路,自写):`body{--sb-thumb: color-mix(in srgb,var(--text) 18%,transparent); --sb-thumb-hover: … 28%; --sb-w:8px}` · `@supports not selector(::-webkit-scrollbar){*{scrollbar-width:thin;scrollbar-color:var(--sb-thumb) transparent}}` · `@supports selector(::-webkit-scrollbar){::-webkit-scrollbar{width:var(--sb-w);height:var(--sb-w)}::-webkit-scrollbar-thumb{background:var(--sb-thumb);border-radius:999px;border:2px solid transparent;background-clip:padding-box}::-webkit-scrollbar-thumb:hover{background:var(--sb-thumb-hover)}::-webkit-scrollbar-track{background:transparent}}`。`main.jsx` import;删 `shell.css:648-654` 旧滚动条段。
- [ ] `theme.css:root` 加 `@supports (corner-shape: superellipse(1.5)){:root{--corner:superellipse(1.5)}*,*::before,*::after{corner-shape:var(--corner)}}`;圆形元素(`border-radius:50%|999px`)所在规则补 `corner-shape:round`。

### B2.2 侧栏(`Rail.jsx` + `Rail.module.css`)

- [ ] `Rail.jsx` 结构**不动**(三段 `rail-fn / rail-scroll / rail-foot` + `rail-new` + `sec-head`),只换 className 为 CSS Modules 并调整内部元素;数值(参照 `SidebarRoot.module.css` 与 `Rows.module.css`):根 `padding: 6px 12px`、`font-size:14px`、`background:var(--bg-panel)`;品牌行 `height:60px; padding:8px 0 8px 4px; margin-bottom:8px`,折叠态 `height:36px; margin-bottom:12px`;**新会话钮** `height:38px; padding:8px 16px; gap:6px; border-radius:12px; font-size:14px; background:var(--bg-element); box-shadow:var(--shadow-1)`;搜索框 `height:36px; border-radius:8px; background:var(--hover)`;功能区行(`fn-item`)`height:36px; padding:0 8px; gap:6px; border-radius:8px`,hover/on 皆 `background:var(--hover)`(DSH 选中=hover 同色);项目/会话行 `min-height:48px; padding:4px 8px; border-radius:8px; font-size:14px; line-height:20px`,二行元信息 `font-size:12px; line-height:17px; color:var(--text-faint)`;页脚图标钮 `28×28 圆; hover var(--hover)`;折叠轨 `56px`、控件 `36×36 r12`、顶部留 18px。
- [ ] 折叠动画:内容 `.fading>*{opacity:0;transition:opacity 150ms var(--ease)}` 先淡出,轨道过渡由 AppFrame grid 承担(B0),淡出结束再切 `.collapsed` 布局(`useEffect` + `transitionend`)。
- [ ] `railCollapsed` 与 `SIDEBAR_AUTO_COLLAPSE=1024` 联动:视口 <1024 自动收轨(不写偏好),手动展开可压过主区(`narrowExpanded` 临时态,不入偏好)。

### B2.3 会话页(`ChatView.jsx` + `SessionHeader.jsx` + `ChatView.module.css`)

- [ ] **新建** `SessionHeader.jsx`:`height:44px; padding:0 16px; gap:8px; border-bottom:.5px solid var(--border-l2)`;左:会话标题(`font-size:14px; font-weight:500`)+ 模式胶囊(现 `.cz-mode` 逻辑上移,`height:24px; padding:1px 8px; border-radius:24px; font-size:13px`);右:「更多」`iconbtn 28` + **「打开右栏」** `iconbtn`(dispatch `rightbar_toggled`)+ **tablist** `对话 | 轨迹`(`role=tablist`,tab `height:28px; padding:0 10px; font-size:13px`,选中 `color:var(--text); border-bottom:2px solid var(--accent)`;**轨迹 tab 点击只切 `chatTab` 态并显示空态文案 `t("chat.trajectory.empty")`**——Q5 默认)。`workbench-state` 加 `chatTab: "chat"|"trajectory"` + action `chat_tab_changed`。
- [ ] 消息流:容器 `padding:16px 32px`(`composer 净空 16 + 16`);列 `max-width:min(100% - 32px, 920px)` 居中;块间 `gap:16px`。**用户消息**:`.u-msg` 改为右对齐列,气泡 `max-width:min(70.2%,82%); background:var(--bubble); border-radius:22px; padding:10px 16px; font-size:14px; line-height:22px; white-space:pre-wrap`。**助手消息**:无气泡,正文 `font-size:14px; line-height:22px`。
- [ ] 卡片(`.ev`)换皮:`border:.5px solid var(--border-l2); border-radius:12px; background:var(--bg-base)`;`.ev-h` `height:32px; padding:0 12px; font-size:13px; color:var(--text-mut)`;`.ev-b` `padding:8px 12px; background:var(--code-bg); border-top:.5px solid var(--border-l1)`。**推理摘要卡**(`ThoughtCard`)改 DSH 折叠行形态:`height:24px` 行、左 2px 竖线 `background:var(--text-faint)`、摘要 `font-size:13px; line-height:20px; color:var(--text-faint)`、展开体缩进 22px。审批卡三态色、diff 卡 add/del 底色**沿用 v1.8.0**(`--ok-bg/--err-bg` 改派生自新语义色)。
- [ ] `HomeView.jsx`:首页问候与快捷卡换皮(卡 `r12 + border-l2 + shadow-1`,hover `shadow-2`);氛围/渐变字保留 v1.8.0 做法。

### B2.4 composer(`Composer.jsx` + `Composer.module.css`)

- [ ] `.cz-in` 改:`border-radius:22px; background:var(--bg-element); border:0; box-shadow:var(--shadow-2); padding:12px 16px 8px`;文本域 `font-size:14px; line-height:24px; min-height:24px`;工具行 `gap:12px; padding-top:8px`,按钮 `28×28 r28`;发送钮 `36×36 圆 accent 底`。**`.cz-meta`(MetricsLine)一字不动**——决策①;仅其容器 `padding-top:8px; font-size:12px; line-height:18px; color:var(--text-faint)`。composer 距列边 `16px`(`--composer-clearance`)。
- [ ] **上下文圆环**(参照 `ContextMeter`,可选但建议):`MetricsLine` 的「上下文 %」段换成 24px SVG 圆环(`stroke-width:2`,底 `var(--hover)`,填 `var(--accent)`)+ 百分比,悬停弹 `r12` 面板显示 tokens/上限——**属 5 形态之外的第 6 形态,须加进 `status-display.js` 的 `STATUS_FORMS`(5→6)并同步 `StatusDisplayPanel`**;不做则跳过本条并在 CHANGELOG 注明。
- [ ] `strings.js` 新键:`chat.trajectory` / `chat.trajectory.empty` / `chat.more` / `rightbar.open` / `rightbar.close`(zh/en)。

### B2 收口

- [ ] 五项准入;预期 ≈1117 → ≈1120(reducer `chat_tab_changed` +1;`STATUS_FORMS` 若改 +1;i18n 抽样 +1)。`tests/unit/gui/status-display.test.js` 若 `STATUS_FORMS` 改 6 需同步断言。**`agent-cards.test.js`(10)、`event-contract.test.js`(14)原样绿**(换皮不动逻辑)。smoke:8 选择器原样(`.cz-input` 仍在)。人工:sumi/paper 两档下会话页、侧栏折叠动画、`/theme` 切配色只变 accent。
- [ ] **提交**:`feat(v1.8.1-b2): 侧栏 + 会话页 — 行/气泡/卡片/composer 全数值对齐 + 会话头 tab + 滚动条皮`。

---

# B3 · 右栏 dock

**目标:** 右栏轨道装 dock(tab 条 + 面板槽),首批三面板:文件树 / 改动 / 恢复。**host 与 IPC 零新增**(`fs:tree fs:read changes:* recovery:*` 已在 `gui/main.js:18-30`)。

### B3.1 文件树纯函数(先测)

- [ ] **写失败测试** `tests/unit/gui/file-tree.test.js` 追加:

```js
import { treeFromPaths } from "../../../gui/src/state/file-tree.js";
test("treeFromPaths builds nested dirs, dirs first, sorted", () => {
  const t = treeFromPaths(["src/b.js", "src/a.js", "README.md", "src/lib/x.js"]);
  assert.deepEqual(t.map((n) => n.name), ["src", "README.md"]);
  assert.deepEqual(t[0].children.map((n) => n.name), ["lib", "a.js", "b.js"]);
  assert.equal(t[0].children[0].children[0].path, "src/lib/x.js");
});
test("treeFromPaths tolerates empty and duplicate input", () => {
  assert.deepEqual(treeFromPaths([]), []);
  assert.equal(treeFromPaths(["a.js", "a.js"]).length, 1);
});
```

- [ ] 跑到失败 → 实现 `treeFromPaths(paths) → [{ name, path, type:"directory"|"file", children? }]`(目录优先、同类按名排序;`file-tree.js` 已存在,追加导出)→ 跑绿。

### B3.2 dock 状态(先测)

- [ ] **写失败测试** `tests/unit/gui/dock-state.test.js`:`rightbar_toggled` 翻转 `rightbarOpen` 且首次打开时 `rightbarWidth===0` 则设为 `Math.round(viewport*0.45)`(action 带 `viewport`);`dock_tab_changed` 白名单外值忽略;`expanded` 目录集合 action `tree_dir_toggled {path}` 在 `dockFiles.expanded` 增删。跑到失败 → `workbench-state.js` 实现 → 跑绿。

### B3.3 Dock 容器与三面板

- [ ] **新建** `Dock.jsx` + `Dock.module.css`(参照 `dockkit.module.css` 数值):面板 `position:absolute; right:0; top:40px; bottom:0; width:${rightbarWidth}px; background:var(--bg-base); border-left:.5px solid var(--border-l3); z-index:10`;tab 条 `height:28px; padding:10px 6px 0 10px; gap:4px`,tab `height:28px; padding:0 10px; border-radius:8px 8px 0 0; font-size:13px`,选中 `background:var(--bg-base); color:var(--text)`,未选 `color:var(--text-mut)`,hover `var(--hover)`;`role=tablist` / `role=tab aria-selected`;右上 关闭 `iconbtn 28`(dispatch `rightbar_toggled`)。**不做**拖放重排。
- [ ] **`FilesPanel.jsx`**:挂载时 `kernel.listTree()`(已有 preload `listTree`)→ `treeFromPaths` → 树行 `height:28px; padding-left: 8px + depth*16px; gap:6px; border-radius:6px; font-size:13px`,目录行 `aria-expanded`,lucide `Folder/FolderOpen/File 14`;点文件 → 现有 `file_opened` 流程(`DiffView` 只读)。
- [ ] **`ChangesPanel.jsx`** / **`RecoveryPanel.jsx`**:把 `SecondaryViews.jsx` 的 `ChangesView`(`:77`)与 `RecoveryView`(`:184`)**移动**为面板(逻辑不变,容器改 dock 内滚动区 `padding:8px 10px`);`SecondaryViews.jsx` 只剩 `ProjectsView / McpView / PluginsView`。
- [ ] `App.jsx`:`<AppFrame rightbar={state.rightbarOpen ? <Dock…/> : null}>`;侧栏功能区的「改动 / 恢复」两项点击改为 `rightbar_toggled + dock_tab_changed`(不再切主区视图);`view === "changes" | "recovery"` 路由**删除**;快捷键 `Ctrl/⌘+J` 正式化为右栏开关(替代 B0 临时键)。
- [ ] `strings.js`:`dock.files / dock.changes / dock.recovery / dock.close`(zh/en);`rail.changes / rail.recovery` 文案不变。

### B3 收口

- [ ] 五项准入;预期 ≈1120 → ≈1126(file-tree +2,dock-state +3,reducer 路由删除相关 −0/+1)。**`tests/unit/gui/kernel-host-changes.test.js`、`changes-derive.test.js` 原样绿**(数据层不动)。smoke:`.rail-fn .fn-item:nth-child(3)` 点击现在开右栏而非切视图——`gui/main.js:168` 的 `shoot("shell-changes")` 仍有效(截到右栏);`nth-child(2)` 项目视图不变。人工:三面板切换、拖右栏 300–70%、<900px 右栏自动让位。
- [ ] **提交**:`feat(v1.8.1-b3): 右栏 dock — 文件树/改动/恢复三面板 + 拖拽轨道`。

---

# B4 · 设置模态 + 收口

**目标:** 设置从全窗改模态(推翻 v1.4 ⑧);删自绘标题栏与 `shell.css`;重做守卫与冒烟基线;发版文档。

### B4.1 设置模态

- [ ] **新建** `SettingsModal.jsx` + `.module.css`:遮罩 `background:color-mix(in srgb,var(--bg-base) 72%,transparent); backdrop-filter:blur(6px)`(`[glass="off"]` 不模糊);卡 `width:min(880px, 100vw - 48px); height:min(640px, 100vh - 48px); border-radius:12px; background:var(--bg-base); box-shadow:var(--shadow-2); border:.5px solid var(--border-l3)`;左 nav `width:200px; padding:12px; background:var(--bg-panel)`,项 `height:32px; padding:0 10px; border-radius:8px; font-size:13px`;右内容 `padding:20px 24px; overflow:auto`;顶部右 关闭 `28 圆`;`role=dialog aria-modal`,Esc 关,点遮罩关(与敏感模态相反——敏感模态**仍不允许**点遮罩关)。
- [ ] `Settings.jsx`:去掉全窗骨架(`.settings / .s-nav / .s-body / .settings-back`),改为导出 `SettingsPanels`(nav 列表 + 面板 switch),由 `SettingsModal` 包裹;`Appearance.jsx` 吸收 `ThemeHub.jsx`(删后者)。`App.jsx`:`view === "settings"` 路由删除,改 `state.settingsOpen` + action `settings_toggled`;`Ctrl/⌘+,` 打开;`.shell-settings` 类删除。
- [ ] **删** `TitleBar.jsx`:语言切换 → 设置›通用;主题明暗 → 设置›外观顶部 + 快捷键 `Ctrl/⌘+Shift+L`;菜单(查看/帮助)→ 侧栏页脚「更多」弹层(`.menu-dropdown` 样式迁 CSS Modules)。`gui/main.js:219` 起的窗控 IPC 与 preload `window:*` 三条**删除**,`IPC_CHANNELS` 同步去掉。
- [ ] `shell.css` 剩余段全部迁入对应 `.module.css` 后**删除文件**;`theme.css` 只留 `@import tokens` + `:root` + `[theme]` 别名块 + `body/:focus-visible`。

### B4.2 守卫与冒烟基线重做

- [ ] **重写** `tests/unit/gui/dom-contract.test.js`(β 后新冻结清单):`AppFrame.jsx` 含 `data-windows-titlebar` 与三槽;`Rail.jsx` 仍含 `rail-fn / rail-scroll / rail-foot / rail-new`(决策②);`MetricsLine.jsx` 含 `cz-meta` 与 `data-mv=`(决策①③);`SensitiveNoticeModal.jsx` 拒绝键在前 + `autoFocus` + 不可点遮罩关;`theme.css` 含 `--titlebar:40px`;`gui/main.js` 含新 smoke 门 8 选择器;**`App.jsx` 不含 `TitleBar` / `shell-settings` / `view === "settings"` / `view === "changes"` / `view === "recovery"`**。
- [ ] `gui/main.js` smoke 门 8 选择器改为:`.ide` · `[data-windows-titlebar]` · `.rail` · `.pane` · `.rail-fn` · `.cz-input` · `[data-rightbar-col]` · `.rail-foot`;点击链:`.rail-new` → chat · `.rail-fn .fn-item:nth-child(2)` → projects · `.rail-fn .fn-item:nth-child(3)` → 右栏改动(截 `shell-changes`)· `Ctrl+,` 或 `.rail-foot .iconbtn:last-child` → 设置模态(截 `shell-settings`,`.settings-nav .item:nth-child(3)` → `shell-appearance`,`:nth-child(4)` → `shell-status-display`,Esc 关)· 敏感模态构造事件(沿用 v1.7.2)· `setSize(800,720)` → `shell-narrow`。`tests/e2e/gui-smoke.test.js` 若引用了旧选择器同步改。
- [ ] 截图基线:sumi / paper 两档 × 9 景 = 18 张入 `docs/prototypes/v1.8.1-dsh-shell/screenshots/`(**不复用** v1.8.0 目录;偏好文件写入用无 BOM 方式——v1.8.0 教训)。

### B4.3 版本与文档

- [ ] 版本四处 → `1.8.1`(`package.json:3` · `package-lock.json` 2 处 · `src/theme.js:2` · `gui/src/App.jsx` `VERSION`)。
- [ ] 文档按维护顺序:spec 状态行改「已实现」+ §8 写入最终采用值;本文 checkbox 勾选 + 执行记录;`project-overview.md` §15(壳层三列 / dock / 设置模态 / 主题 C 变体 / 偏好键 `sidebarWidth rightbarWidth rightbarOpen dockTab chatTab`);v1.4 spec §2 决策⑧ 加「**v1.8.1 推翻**:设置改模态,理由见 v1.8.1 spec §4.1」附注;v1.8.0 spec §3 加「**v1.8.1 更替**:四段带退场,C 变体见 v1.8.1 spec §3」;`docs/CHANGELOG.md` `[Unreleased]` → `## v1.8.1 — <日期> · 壳层重构(三列 dock + 设置模态 + 中性阶梯)`;`docs/README.md` 当前版本行 + 索引登记本 plan。
- [ ] 死钩子清扫:`strings.js` 里 `TitleBar` 专用键(`toggle.theme / toggle.lang / menu.*` 中不再引用者)、`settings-back`、`shell-settings`。

### B4 收口

- [ ] 五项准入 + e2e ×4(cli/tui/gui-build/gui-smoke);预期 ≈1126 → ≈1128(settings_toggled +1;dom-contract 重写 ±0;删 ThemeHub 测试若有则 −N 并在 CHANGELOG 注明)。**归属词自检**:`git diff main..HEAD | grep -iE "mimo|由 claude|subagent|superpowers|co-authored|@deepseek-ai/"` 为零。
- [ ] **提交**:`chore(v1.8.1): 收口 — 设置模态 + 删自绘标题栏 + 守卫/冒烟基线重做 + 截图 + 版本文档`,推 `feat/v1.8`。**合入 main、打 tag `v1.8.1`、推 main 与 tag 由维护者做。**

---

## 附录 A · 设计事实取值索引(执行者查表用;只读参照,不复制)

| 事实 | DSH 参照文件:行 | Inkstone 落点 |
|---|---|---|
| 列宽 9 常量 | `ui-layout/src/client/columns.ts:11-27` | `gui/src/state/columns.js` |
| 三列 grid / 手柄 / Windows 圆角 | `ui-layout/src/client/AppFrame.module.css:1-60, 95-120` | `AppFrame.module.css` |
| 标题栏 40 + overlay 色 | `apps/desktop/src/windows-layout.ts:4`;`main.ts:119-121` | `gui/main.js` |
| 中性阶梯 / 品牌蓝 / 语义色 | `ui-theme/src/styles/design-platform.css:53-74`(static)`:157-244`(明 alias)`:254-341`(暗 alias) | `tokens.css` 结构槽;`theme.css` 派生 |
| 阴影 lv1/lv2 | `ui-theme/src/styles/gradient-shadow-text.css:5,7` | `theme.css --shadow-1/2` |
| 动效 / 字体 | `ui-theme/src/styles/base.css:7-14` | `theme.css :root` |
| 滚动条双路径 | `ui-theme/src/styles/scrollbar.css`(思路) | `gui/src/styles/scrollbar.css` |
| 超椭圆圆角 | `ui-theme/src/styles/corner-shape.css` | `theme.css` |
| 首帧防闪 | `ui-theme/src/boot-theme.ts:11-33` | `gui/index.html` + `main.js` |
| 侧栏根 / 品牌行 / 折叠 / 轨 | `ui-sidebar/src/client/SidebarRoot.module.css:9-30, 67-83, 140-160` | `Rail.module.css` |
| 新会话钮 / 图标钮 | 同上 `:109-123, 36-61`(相对 260 行偏移) | `Rail.module.css` |
| 项目/会话行 | `ui-workspace/src/client/rows/Rows.module.css:1-20, 24-35, 60-73` | `Rail.module.css` |
| 用户气泡 / 助手正文 | `ui-chat/src/client/chat/MessageItem.module.css:4-40` | `ChatView.module.css` |
| 消息流宽 / gap | `ui-chat/src/client/chat/ChatView.module.css:15, 39, 63`;`--dsh-chat-content-width` 定义处 | `ChatView.module.css` |
| 思考折叠行 | `ui-chat/src/client/chat/ReasoningRow.module.css:8, 51-76` | `ChatView.module.css`(ThoughtCard) |
| composer 卡 | `ui-conversation/src/client/skeleton/InputBar.module.css:10-69` | `Composer.module.css` |
| 上下文圆环 | `ui-conversation/src/client/skeleton/ContextMeter.module.css:9-58` | `MetricsLine.jsx`(第 6 形态) |
| dock tab 条 / 面板 | `ui-dockkit/src/components/dockkit.module.css:133-270` | `Dock.module.css` |
| 文件树行模型 | `ui-sidebar-files/src/client/FilesBody.tsx:48, 103-125`(展开态 + 排序;数据源不同) | `file-tree.js` + `FilesPanel.jsx` |
| 设置模态 | `docs/user/guide/providers-models-page.zh.png`(截图)+ `ui-settings-general/src/client/chrome.module.css` | `SettingsModal.module.css` |
