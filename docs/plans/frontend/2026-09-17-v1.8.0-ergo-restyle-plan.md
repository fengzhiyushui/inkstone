# v1.8.0 工学换肤(ergo-restyle)实施方案

> 完成状态以 [CHANGELOG](../../CHANGELOG.md) 为准。
> **状态:✅ 完整可执行(2026-09-17)。附录 A 六套色值已回填并经三方复算 66/66 通过。前置 v1.7.2 合入后即可从 P0.1 开工。**
> 目标版本 **v1.8.0**(minor);分支 `feat/v1.8`;每层一个完整提交,提交信息不带 `Co-Authored-By`。
> 前置:**v1.7.2 GUI 缺陷补丁必须先合入**(P2 依赖 assistant 回复入流);见 [post-V3 路线图 §3](../../specs/architecture/2026-09-17-post-v3-roadmap-design.md)。
> spec:[2026-09-17-v1.8.0-ergo-restyle-design.md](../../specs/frontend/2026-09-17-v1.8.0-ergo-restyle-design.md)(已评审,四项确认见其 §8)。

**执行者须知:** 本文所有 `文件:行` 均在 `main @ 2f35c6f`(v1.7.2)核实,相对仓库根 `d:\person studio\deepseek code\`。若行号漂移以**内容锚点**为准(每处都给了要找的字符串)。严格 TDD:先写失败测试 → 跑到它失败 → 最小实现 → 跑绿 → 提交。**不得触碰** `src/core src/deepseek src/tools src/edits src/sessions src/context src/security src/workspace` 八个目录(每层收口用 `git diff --stat main..HEAD -- <八目录>` 证明为空)。

**环境与流程约束(v1.7.2 执行经验,2026-09-19):**
- **分支**:在主 worktree 上 `git checkout -b feat/v1.8 main` 开工;**不要** `git worktree add`(会话钩子禁止,会改共享 ref 库)。
- **推送**:每层只 `git push origin feat/v1.8`(首次加 `-u`);**不推 main、不打 tag**——合入 main 与打 tag 由维护者做。plan 里凡写「提交」都指提交到 `feat/v1.8` 并推该分支。
- **路径**:CHANGELOG 在 `docs/CHANGELOG.md`(仓库根没有 CHANGELOG.md);文档索引在 `docs/README.md`。
- **计数口径**:本文凡写「N 处」指**引用次数**,一行含多个即计多个;写「N 行」才指行号数。
- **设置页无 Rail**:`view === "settings"` 时 `.rail` 不渲染(v1.4.7 全窗化),从设置页要回其它视图须先 `click(".settings-back")`;直接 `click(".rail-fn …")` 会静默失败。

---

## 全局约束

- 测试基线:v1.7.1 实测 **1085**;本 plan 假定 **v1.7.2 已合入(基线 1089)**,各层预期数以此为起点。只增不减。
- 每层收口五项准入,缺一不过:① `npm test` ② `npm run check` ③ `cd gui && npm run build:renderer` ④ `DEEPSEEK_CODE_GUI_SMOKE=1 node --test tests/e2e/gui-smoke.test.js` 输出含 `GUI_SMOKE_READY` ⑤ 八目录 diff 为空。
- DOM 与类名**冻结**(spec §5 冻结清单);P0 加字符串守卫测试让"顺手改类名"在单测层变红。
- 为 β 留门(spec §6):新类名只取映射表;`shell.css` 新规则**只追加到文件末尾**独立区块 `/* ── v1.8 γ ── */`;派生变量进 `theme.css` 的 `[theme]` 别名块;偏好键定名 `lastDark / lastLight / glass` 三键。
- 视觉规则:字号 ≥11px 且落在 `--fs-11..18`;图标只用 `lucide-react`(12 / 14 / 16 三档);零 emoji;全部文案进 `strings.js` zh/en;可点击元素一律 `<button type="button">`;无 `transition: all`;新增动效 ≤ .25s 且 `prefers-reduced-motion` 关。

---

## 文件结构总览

| 层 | 新建 | 修改 |
|---|---|---|
| P0 | `scripts/check-theme-contrast.mjs` · `tests/unit/gui/theme-contrast.test.js` · `tests/unit/gui/theme-pins.test.js` · `tests/unit/gui/dom-contract.test.js` | `gui/src/styles/tokens.css`(重写)· `gui/src/state/themes.js` · `gui/src/state/workbench-state.js` · `gui/kernel-host.js` · `gui/src/components/DiffView.jsx` · `src/apps/tui/tui-app.js` · `scripts/gen-tui-theme.js` · `src/apps/tui/theme-palette.js`(生成物)· `gui/src/i18n/strings.js` · 5 个既有测试 |
| P1 | `gui/src/state/theme-hub.js` · `tests/unit/gui/theme-hub.test.js` | `gui/src/styles/theme.css` · `gui/src/styles/shell.css`(末尾追加)· `gui/src/App.jsx` · `gui/src/components/v4/Rail.jsx`(仅加一个图标)· `strings.js` |
| P2 | `gui/src/components/v4/ThoughtCard.jsx`(可并入 ChatView)· `gui/src/components/Settings/ThemeHub.jsx` | `gui/src/components/v4/ChatView.jsx` · `gui/src/state/agent-cards.js` · `src/apps/event-contract.js` · `gui/src/hooks/kernel-loads.js` · `gui/src/hooks/useKernel.js` · `gui/src/components/Settings/Appearance.jsx`(改为挂 ThemeHub)· `HomeView.jsx` · `shell.css` · `strings.js` · 4 个既有测试 |
| P3 | `docs/prototypes/v1.8.0-ergo-restyle/screenshots/*.png`(16 张) | 版本四处 · CHANGELOG · docs/README · project-overview · v1.4 spec §3 附注 · 本文状态 · `shell.css`/`strings.js` 死钩子 |

---

# P0 · tokens 地基

**目标:** 10 主题变成四段带(暗 3 / 柔暗 2 / 柔明 2 / 明 3),闸门变常驻单测,三端主题 id 单源可证,退役 6 套静默迁移。**完成后界面看不出变化,只有主题列表变了。**

### 接口(P1/P2 依赖)

- `gui/src/state/themes.js` 导出:`GUI_THEMES`(10 条,顺序即 UI 顺序,每条 `{ id, name, tier, group, family }`)· `themeLabel(id)` · `isLightTheme(id)`(语义不变:仅 `group==="light"`)· **新增** `TIERS = ["dark","dim","tint","light"]` · **新增** `themesByTier()` → `{ dark:[…], dim:[…], tint:[…], light:[…] }`
- `gui/src/state/workbench-state.js` 状态新增 `lastDark`(默认 `"sumi"`)、`lastLight`(默认 `"latte"`)、`glass`(默认 `true`);新 action `glass_changed`;`theme_changed` 同步 lastDark/lastLight
- `gui/kernel-host.js` 偏好白名单新增同三键
- `scripts/check-theme-contrast.mjs` 导出 `checkThemes(css) → { ok, rows:[{theme,pair,value,min,max,pass}] }`

### P0.1 闸门脚本(先于取色,TDD 的"测")

- [ ] **写失败测试** `tests/unit/gui/theme-contrast.test.js`:

```js
import test from "node:test";
import assert from "node:assert/strict";
import { readFileSync } from "node:fs";
import { checkThemes, contrastRatio } from "../../../scripts/check-theme-contrast.mjs";

test("contrastRatio reproduces v1.4 recorded values", () => {
  assert.equal(contrastRatio("#d3c6aa", "#2d353b").toFixed(2), "7.38"); // forest(退役前)
  assert.equal(contrastRatio("#dcd7ba", "#1f1f28").toFixed(2), "11.26"); // sumi
  assert.equal(contrastRatio("#1c1b1a", "#fffcf0").toFixed(2), "16.73"); // paper
});

test("all 10 themes pass all 11 gate pairs (110/110)", () => {
  const css = readFileSync(new URL("../../../gui/src/styles/tokens.css", import.meta.url), "utf8");
  const r = checkThemes(css);
  const failed = r.rows.filter((x) => !x.pass);
  assert.equal(r.rows.length, 110, "应为 10 主题 × 11 对");
  assert.deepEqual(failed, [], failed.map((x) => `${x.theme} ${x.pair}=${x.value}`).join("\n"));
});

test("tokens.css has exactly 10 top-level theme blocks with 18 slots each, 12 palette slots as #rrggbb", () => {
  const css = readFileSync(new URL("../../../gui/src/styles/tokens.css", import.meta.url), "utf8");
  const blocks = [...css.matchAll(/\[theme="(\w+)"\]\s*\{([^}]*)\}/g)];
  assert.equal(blocks.length, 10);
  const SLOTS = ["bg-base","bg-panel","bg-element","bg-inset","text","text-mut","text-faint","border","border-strong","accent","accent-hover","accent-soft","accent-text","ok","warn","err","info","code-bg"];
  const HEX = ["bg-base","bg-panel","text","text-mut","text-faint","border","accent","accent-hover","ok","warn","err","info"];
  for (const [, id, body] of blocks) {
    for (const s of SLOTS) assert.match(body, new RegExp(`--${s}\\s*:`), `${id} 缺 --${s}`);
    for (const s of HEX) assert.match(body, new RegExp(`--${s}\\s*:\\s*#[0-9a-f]{6};`), `${id} --${s} 须为小写 #rrggbb(TUI 生成器契约)`);
  }
});
```

- [ ] **跑到失败**:`node --test tests/unit/gui/theme-contrast.test.js` → 预期 `ERR_MODULE_NOT_FOUND`(脚本还不存在)
- [ ] **实现** `scripts/check-theme-contrast.mjs`:

```js
// scripts/check-theme-contrast.mjs — v1.8.0 主题对比度闸门(v1.4 spec §3.2 的 11 对 × 10 套 = 110 项)。
// 用法:node scripts/check-theme-contrast.mjs  → 打印表格,任一不过 exit 1。被测试 import 时只导出纯函数。
import { readFileSync } from "node:fs";
import { fileURLToPath, pathToFileURL } from "node:url";
import { dirname, join } from "node:path";

export function relLum(hex) {
  const v = hex.replace("#", "");
  const c = [0, 2, 4].map((i) => parseInt(v.slice(i, i + 2), 16) / 255)
    .map((x) => (x <= 0.03928 ? x / 12.92 : ((x + 0.055) / 1.055) ** 2.4));
  return 0.2126 * c[0] + 0.7152 * c[1] + 0.0722 * c[2];
}
export function contrastRatio(a, b) {
  const [hi, lo] = [relLum(a), relLum(b)].sort((p, q) => q - p);
  return (hi + 0.05) / (lo + 0.05);
}
// 11 对:[名, 前景槽, 背景槽, min, max]
export const GATE = [
  ["text/bg-base", "text", "bg-base", 7, 19.5],
  ["text/bg-panel", "text", "bg-panel", 7, 19.5],
  ["text-mut/bg-base", "text-mut", "bg-base", 4.5, Infinity],
  ["text-faint/bg-base", "text-faint", "bg-base", 3, Infinity],
  ["accent/bg-base", "accent", "bg-base", 3, Infinity],
  ["accent-text/accent", "accent-text", "accent", 4.5, Infinity],
  ["ok/bg-base", "ok", "bg-base", 3, Infinity],
  ["warn/bg-base", "warn", "bg-base", 3, Infinity],
  ["err/bg-base", "err", "bg-base", 3, Infinity],
  ["info/bg-base", "info", "bg-base", 3, Infinity],
  ["border-strong/bg-base", "border-strong", "bg-base", 1.5, Infinity]
];
export function parseThemes(css) {
  return [...css.matchAll(/\[theme="(\w+)"\]\s*\{([^}]*)\}/g)].map(([, id, body]) => {
    const vars = {};
    for (const [, k, v] of body.matchAll(/--([\w-]+)\s*:\s*([^;]+);/g)) vars[k] = v.trim();
    return { id, vars };
  });
}
export function checkThemes(css) {
  const rows = [];
  for (const { id, vars } of parseThemes(css)) {
    for (const [pair, fg, bg, min, max] of GATE) {
      const value = +contrastRatio(vars[fg], vars[bg]).toFixed(2);
      rows.push({ theme: id, pair, value, min, max, pass: value >= min && value <= max });
    }
  }
  return { ok: rows.every((r) => r.pass), rows };
}
const isDirect = process.argv[1] && import.meta.url === pathToFileURL(process.argv[1]).href;
if (isDirect) {
  const root = dirname(dirname(fileURLToPath(import.meta.url)));
  const r = checkThemes(readFileSync(join(root, "gui/src/styles/tokens.css"), "utf8"));
  for (const x of r.rows) console.log(`${x.pass ? "✓" : "✗"} ${x.theme.padEnd(7)} ${x.pair.padEnd(22)} ${x.value}`);
  console.log(r.ok ? "110/110 PASS" : `FAIL: ${r.rows.filter((x) => !x.pass).length} pairs`);
  process.exit(r.ok ? 0 : 1);
}
```

- [ ] **跑**:第一条(锚点)绿;第二条对**现行** tokens.css 应 110/110 绿(v1.4 已过闸)——若红,说明公式错,先修脚本再往下。第三条现行也应绿。
- [ ] `package.json:12` 的 `check` 脚本追加 ` src/apps/tui/theme-palette.js` 之后:` && node --check scripts/check-theme-contrast.mjs scripts/gen-tui-theme.js`

### P0.2 重写 `tokens.css`(六套新色值见附录 A)

- [ ] 文件头注释(`tokens.css:1-8`,含 "DeepSeek Code v1.4.0")改为:

```
/* Inkstone v1.8.0 设计系统 — 单一 token 源,10 主题四段带(暗 3 · 柔暗 2 · 柔明 2 · 明 3)
   块顺序 = 设置›外观 UI 顺序 = TUI /theme 轮换顺序(从深到浅连续过渡)。
   对比度闸门 11 对 × 10 = 110 项由 scripts/check-theme-contrast.mjs 常驻单测守护(v1.4 §3.2 + 描边 ≥1.5)。
   保留(色值不动):sumi Kanagawa Wave · nord Nord · paper Flexoki Light · latte Catppuccin Latte。
   新增:slate Tokyo Night Storm · vesper Vesper(底提亮至 L≥25)· ash Zenburn · snow Nord Snow Storm ·
        sand Gruvbox Light Soft · lotus Kanagawa Lotus。每处偏离官方值见块内注释。
   退役 6 套(dawn/mocha/moon/forest/clay/rose)→ 迁移表见 workbench-state.js LEGACY_THEME_MAP。 */
```

- [ ] `:root`(`:9-15`)**不动**。删除 `:17` 与 `:49` 的"明色 3 套/暗色 7 套"注释行。
- [ ] 主题块**按此顺序重排**,保留 4 套色值**逐字节不动**(从现行文件原样搬):

| 序 | id | 档 | 处置 |
|---|---|---|---|
| 1 | `sumi` 墨 | dark | 原样(`tokens.css:52-60`) |
| 2 | `slate` 玄 | dark | 新,色值见附录 A |
| 3 | `vesper` 烬 | dark | 新,色值见附录 A |
| 4 | `nord` 峡 | dim | 原样(`:82-90`) |
| 5 | `ash` 灰 | dim | 新,色值见附录 A |
| 6 | `snow` 霜 | tint | 新,色值见附录 A |
| 7 | `sand` 沙 | tint | 新,色值见附录 A |
| 8 | `lotus` 荷 | light | 新,色值见附录 A |
| 9 | `latte` 瓷 | light | 原样(`:40-48`) |
| 10 | `paper` 宣 | light | 原样(`:20-28`) |

> ⏳→✅ **六套新块的 18 槽色值见本 plan 附录 A**(已回填;每套经三方独立复算 11 对全过)。每块前的注释行已在附录 A 给出,原样搬入。

- [ ] 删 `dawn`(`:30-37`)`mocha`(`:62-69`)`moon`(`:72-79`)`forest`(`:92-99`)`clay`(`:102-109`)`rose`(`:112-119`)六块。
- [ ] **验证**:`node scripts/check-theme-contrast.mjs` 打印 `110/110 PASS`;`node --test tests/unit/gui/theme-contrast.test.js` 三条全绿。

### P0.3 主题 id 钉住点(9 处一次改齐——退 6 加 6 必须原子,否则 `=== 10` 断言红)

- [ ] `gui/src/state/themes.js:2-13` `GUI_THEMES` 换为(顺序 = P0.2 块顺序;新增 `tier`;`group` 由 tier 派生:dark/dim→`"dark"`,tint/light→`"light"`):

```js
export const TIERS = ["dark", "dim", "tint", "light"];
export const GUI_THEMES = [
  { id: "sumi",   name: "墨", tier: "dark",  group: "dark",  family: "Kanagawa Wave" },
  { id: "slate",  name: "玄", tier: "dark",  group: "dark",  family: "Tokyo Night Storm" },
  { id: "vesper", name: "烬", tier: "dark",  group: "dark",  family: "Vesper" },
  { id: "nord",   name: "峡", tier: "dim",   group: "dark",  family: "Nord" },
  { id: "ash",    name: "灰", tier: "dim",   group: "dark",  family: "Zenburn" },
  { id: "snow",   name: "霜", tier: "tint",  group: "light", family: "Nord Snow Storm" },
  { id: "sand",   name: "沙", tier: "tint",  group: "light", family: "Gruvbox Light Soft" },
  { id: "lotus",  name: "荷", tier: "light", group: "light", family: "Kanagawa Lotus" },
  { id: "latte",  name: "瓷", tier: "light", group: "light", family: "Catppuccin Latte" },
  { id: "paper",  name: "宣", tier: "light", group: "light", family: "Flexoki Light" }
];
export function themesByTier() {
  return Object.fromEntries(TIERS.map((t) => [t, GUI_THEMES.filter((x) => x.tier === t)]));
}
```
  `themeLabel` / `isLightTheme` 实现体不变。
- [ ] `gui/src/state/workbench-state.js`:
  - `:10` `THEMES` → `["sumi","slate","vesper","nord","ash","snow","sand","lotus","latte","paper"]`
  - `:11` `LEGACY_THEME_MAP` → `{ night:"sumi", day:"latte", dawn:"lotus", mocha:"sumi", moon:"slate", forest:"ash", clay:"vesper", rose:"sumi" }`
  - `:387` `THEME_LABELS` → `{ sumi:"墨", slate:"玄", vesper:"烬", nord:"峡", ash:"灰", snow:"霜", sand:"沙", lotus:"荷", latte:"瓷", paper:"宣" }`
  - `createInitialState`(`:42` `theme: "sumi"` 旁)追加 `lastDark: "sumi", lastLight: "latte", glass: true`
  - `preferences_loaded`(`:186-196`)追加三行:`lastDark: normalizeTheme(prefs.lastDark, current.lastDark)`(且若结果 `isLight` 则回退 `current.lastDark`——用 `GUI_THEMES`/`THEMES` 无法判 group,故在本文件内加 `var LIGHT_IDS = ["snow","sand","lotus","latte","paper"]` 与 `function isLightId(id){ return LIGHT_IDS.indexOf(id) >= 0; }`)、`lastLight` 对称、`glass: typeof prefs.glass === "boolean" ? prefs.glass : current.glass`
  - `theme_changed`(`:203-205`)改为:
    ```js
    var next = normalizeTheme(action.theme, "sumi");
    return copy(current, isLightId(next) ? { theme: next, lastLight: next } : { theme: next, lastDark: next });
    ```
  - 新增 `if (action.type === "glass_changed") return copy(current, { glass: Boolean(action.glass) });`
- [ ] `gui/kernel-host.js`:
  - `:148` `GUI_THEMES` Set → 同上 10 个 id
  - `:149` `LEGACY_GUI_THEME` → 同 `LEGACY_THEME_MAP`(8 条)
  - `:89-97` `GUI_PREFERENCE_DEFAULTS` 追加 `lastDark: "sumi", lastLight: "latte", glass: true`
  - `:151-162` `normalizeGuiPreferences` 追加:`lastDark: GUI_DARK.has(input.lastDark) ? input.lastDark : "sumi"`、`lastLight: GUI_LIGHT.has(input.lastLight) ? input.lastLight : "latte"`、`glass: typeof input.glass === "boolean" ? input.glass : true`;在 `:148` 旁定义 `const GUI_DARK = new Set(["sumi","slate","vesper","nord","ash"]); const GUI_LIGHT = new Set(["snow","sand","lotus","latte","paper"]);`
- [ ] `gui/src/components/DiffView.jsx:7-9`:删 `LIGHT` 常量,`monacoThemeFor` 改为 `import { isLightTheme } from "../state/themes.js"; export function monacoThemeFor(theme) { return isLightTheme(theme) ? "vs" : "vs-dark"; }`
- [ ] `src/apps/tui/tui-app.js:524-527`(锚点 `prefs.theme && tuiThemeList().some`):前面插入退役映射 `const TUI_LEGACY = { dawn:"lotus", mocha:"sumi", moon:"slate", forest:"ash", clay:"vesper", rose:"sumi" }; const wanted = TUI_LEGACY[prefs.theme] || prefs.theme;` 并把后两行的 `prefs.theme` 改为 `wanted`(3 处)。
- [ ] `scripts/gen-tui-theme.js:12-15` `NAMES` → `{ sumi:"墨", slate:"玄", vesper:"烬", nord:"峡", ash:"灰", snow:"霜", sand:"沙", lotus:"荷", latte:"瓷", paper:"宣" }` 并改为 `export const NAMES`;`:78` 断言 10 不动;`:94` `DEFAULT_TUI_THEME = "sumi"` 不动。
- [ ] 跑 `node scripts/gen-tui-theme.js` 重生成 `src/apps/tui/theme-palette.js`(生成物入库,提交)。
- [ ] `gui/src/i18n/strings.js`:`:112` `"10 套主题 · 3 浅 7 深 · 点击即刻预览"` → `"10 套主题 · 暗 3 · 柔暗 2 · 柔明 2 · 明 3 · 点击即刻预览"`;`:252` 英文 → `"10 themes - dark 3 / dim 2 / tint 2 / light 3 - click to preview"`;`:113/:253` 旁追加 `"settings.appearance.dim": "柔暗"` / `"dim"`、`"settings.appearance.tint": "柔明"` / `"tint"`。

### P0.4 三端主题 id 单源守卫 + DOM 冻结守卫

- [ ] **写测试** `tests/unit/gui/theme-pins.test.js`:

```js
import test from "node:test";
import assert from "node:assert/strict";
import { GUI_THEMES, TIERS, themesByTier, isLightTheme } from "../../../gui/src/state/themes.js";
import { TUI_THEMES, DEFAULT_TUI_THEME } from "../../../src/apps/tui/theme-palette.js";
import { NAMES } from "../../../scripts/gen-tui-theme.js";
import { createInitialState, applyWorkbenchAction } from "../../../gui/src/state/workbench-state.js";

const ids = GUI_THEMES.map((x) => x.id);
const RETIRED = { dawn: "lotus", mocha: "sumi", moon: "slate", forest: "ash", clay: "vesper", rose: "sumi" };

test("GUI / TUI palette / generator NAMES share one id set, in order", () => {
  assert.deepEqual(Object.keys(TUI_THEMES), ids);
  assert.deepEqual(Object.keys(NAMES), ids);
  assert.equal(ids.length, 10);
  assert.equal(DEFAULT_TUI_THEME, "sumi");
});
test("tiers are dark3/dim2/tint2/light3 and group is derived from tier", () => {
  const by = themesByTier();
  assert.deepEqual(TIERS.map((t) => by[t].length), [3, 2, 2, 3]);
  for (const th of GUI_THEMES) assert.equal(isLightTheme(th.id), th.tier === "tint" || th.tier === "light", th.id);
});
test("retired ids migrate and never survive normalize", () => {
  for (const [old, next] of Object.entries(RETIRED)) {
    assert.ok(!ids.includes(old), `${old} 仍在集合`);
    const s = applyWorkbenchAction(createInitialState(), { type: "theme_changed", theme: old });
    assert.equal(s.theme, next, `${old}→${next}`);
  }
});
test("theme_changed keeps lastDark / lastLight in sync by group", () => {
  let s = createInitialState();
  s = applyWorkbenchAction(s, { type: "theme_changed", theme: "snow" });
  assert.equal(s.lastLight, "snow"); assert.equal(s.lastDark, "sumi");
  s = applyWorkbenchAction(s, { type: "theme_changed", theme: "ash" });
  assert.equal(s.lastDark, "ash"); assert.equal(s.lastLight, "snow");
});
```

- [ ] **写测试** `tests/unit/gui/dom-contract.test.js`(字符串守卫,读源文本):

```js
import test from "node:test";
import assert from "node:assert/strict";
import { readFileSync } from "node:fs";
const read = (p) => readFileSync(new URL(`../../../${p}`, import.meta.url), "utf8");

test("frozen DOM/CSS contracts survive (v1.8 γ 冻结清单)", () => {
  const app = read("gui/src/App.jsx"), rail = read("gui/src/components/v4/Rail.jsx");
  const metrics = read("gui/src/components/v4/MetricsLine.jsx"), sn = read("gui/src/components/v4/SensitiveNoticeModal.jsx");
  const themeCss = read("gui/src/styles/theme.css"), main = read("gui/main.js");
  assert.match(app, /setAttribute\("theme", state\.theme\)/);
  assert.match(app, /className=\{`shell\$\{state\.railCollapsed \? " rail-off" : ""\}\$\{view === "settings" \? " shell-settings" : ""\}`\}/);
  for (const cls of ["rail-fn", "rail-scroll", "rail-foot", "rail-new"]) assert.match(rail, new RegExp(`className="${cls}`), cls);
  assert.match(metrics, /className="cz-meta/);
  assert.match(metrics, /data-mv=/);
  assert.match(sn, /className="sn-backdrop" role="dialog" aria-modal="true"/);
  assert.ok(sn.indexOf("sn-refuse") < sn.indexOf("sn-allow"), "拒绝键须在允许键之前");
  assert.match(sn, /sn-refuse" autoFocus/);
  assert.match(themeCss, /--titlebar:30px/);
  for (const sel of [".ide", 'header[role="banner"]', ".shell", ".rail", ".pane", ".rail-fn", ".cz-input", ".titlebar .actions .lang"])
    assert.ok(main.includes(`querySelector(${JSON.stringify(sel)})`) || main.includes(`querySelector('${sel}')`), `smoke 门选择器 ${sel}`);
  assert.ok(!/telemetry|statusbar/.test(app), "不得出现遥测条/独立状态栏");
});
```

- [ ] **改既有断言 5 处**(退役 id 不再存在):
  - `tests/unit/gui/themes.test.js:20` `themeLabel("rose")==="黛"` → `themeLabel("vesper")==="烬"`
  - `tests/unit/gui/workbench-state.test.js:99,105` `"mocha"` → `"slate"`(两处同一测试);`:146` `themeLabel("rose")==="黛"` → `themeLabel("lotus")==="荷"`
  - `tests/unit/gui/kernel-host.test.js:312-322`(测试「gui preferences normalize invalid values to safe defaults」)与 `:335-343`(「load missing corrupt and save sanitized values」)的 `deepEqual` 期望对象各补 `lastDark: "sumi", lastLight: "latte", glass: true`
- [ ] `tests/unit/gui/gui-preferences-language.test.js` 追加 1 条:`normalizeGuiPreferences({ lastDark: "snow", lastLight: "sumi", glass: "yes" })` → `{ lastDark: "sumi", lastLight: "latte", glass: true }`(跨组值与非布尔全部回默认)。
- [ ] `tests/unit/gui/workbench-state.test.js` 追加 1 条:`preferences_loaded` 带 `{ lastDark: "ash", lastLight: "sand", glass: false }` 后状态三字段对应。

### P0 收口

- [ ] 五项准入全绿;预期测试数 **1089 → ≈1098**(+3 contrast +4 pins +1 dom-contract +1 prefs +1 reducer)。
- [ ] 不受影响但必须证明仍绿:`tests/unit/apps/tui-theme-palette.test.js`(仍 10、仍 sumi)、`tests/unit/gui/i18n.test.js`、`tests/unit/gui/kernel-loads.test.js`。
- [ ] **提交**:`feat(v1.8.0-p0): tokens 四段带 + 对比度常驻闸门 + 三端主题单源守卫`

---

# P1 · 壳层视觉层(DOM 冻结,只改 CSS + 主题中枢)

**目标:** "新皮肤"。侧栏/标题栏/composer/滚动条/模态换视觉;明暗切换有记忆;玻璃态只给浮层。

### 接口

- `gui/src/state/theme-hub.js` 导出纯函数:`nextInGroup(id)`(同 group 内循环下一个)· `otherModeTheme({ theme, lastDark, lastLight })`(返回另一模式最近主题)· `groupOf(id)`

### P1.1 主题中枢

- [ ] **写测试** `tests/unit/gui/theme-hub.test.js`(4 条):`nextInGroup("sumi")==="slate"`、`nextInGroup("ash")==="sumi"`(暗组末尾回绕)、`nextInGroup("paper")==="snow"`;`otherModeTheme({theme:"sumi",lastDark:"sumi",lastLight:"sand"})==="sand"`、反向 `"ash"`;`groupOf("nord")==="dark"`、`groupOf("snow")==="light"`;退役 id 输入 `nextInGroup("rose")` 不抛且返回 `"slate"`(按 sumi 处理)。
- [ ] **实现** `theme-hub.js`(基于 `GUI_THEMES` + `LEGACY` 归一;≤30 行)。
- [ ] `gui/src/App.jsx:85-90` `cycleTheme` 改为 `nextInGroup(state.theme)`(状态行主题标签点击不再跨明暗);`:92-96` `toggleTheme` 改为 `otherModeTheme(state)`;两处 `kernel.setPreferences({ theme: next })` 不变——**lastDark/lastLight 由 reducer 的 `theme_changed` 同步,但要落盘**:在 `toggleTheme`/`cycleTheme`/Appearance 的 `setTheme` 里改为 `kernel.setPreferences({ theme: next, ...(isLightTheme(next) ? { lastLight: next } : { lastDark: next }) })`。
- [ ] `TitleBar.jsx:60-61` 不改(已按 `isLightTheme` 判图标)。

### P1.2 标题栏令牌化(`gui/src/styles/theme.css`)

- [ ] `:40,44,48,54` 四处 `#8884` → `color-mix(in srgb, var(--text) 12%, transparent)`;`:49` `#e81123` → `var(--err)`,`#fff` → `var(--accent-text)`;`:55` `#0007` → `color-mix(in srgb, var(--bg-inset) 55%, transparent)`。
- [ ] `:32` `:focus-visible{outline:1px …}` **删除**(与 `tokens.css:133` 的 2px 版冲突,theme.css 后加载盖住了它)。
- [ ] `:26-30` `body` 的 `font-family` 改为 `var(--font-ui)`(现在 `--font-ui` 定义了但 body 没用它)。

### P1.3 侧栏 / composer / 滚动条(`shell.css` **末尾追加**,区块注释 `/* ── v1.8 γ · P1 ── */`)

- [ ] 侧栏:`.rail-brand .lg` 加 `border: 1px solid var(--border-strong); box-shadow: 0 0 0 3px var(--accent-glow)`;`.fn-item.on` 改 `background: var(--accent-soft); border: 1px solid color-mix(in srgb, var(--accent) 35%, transparent)`;`.r-item .xx` 改 `opacity: .7`(时间戳常显淡色);`.new-menu` 改 `background: var(--bg-glass)` + `[glass="on"] .new-menu { backdrop-filter: blur(12px); }`;`:73` 的 `rgba(0,0,0,.32)` 由新规则 `box-shadow: 0 10px 32px color-mix(in srgb, var(--bg-inset) 60%, transparent)` 覆盖。
- [ ] `Rail.jsx:104-106`(锚点 `rail-search`)的 `<input>` 前加 `<Search size={12} aria-hidden="true" />`(lucide;不改 input 类名)。
- [ ] composer:`.cz-in:focus-within { box-shadow: 0 20px 50px -10px var(--accent-glow); }`;`[glass="on"] .view-chat .cz-in { background: var(--bg-glass); backdrop-filter: blur(10px); }`;`:365` 的 `rgba(0,0,0,.08)` 由新规则覆盖为 `color-mix(in srgb, var(--bg-inset) 30%, transparent)`。**`.cz-meta` 一字不动**(v1.4 决策①)。
- [ ] 滚动条:`::-webkit-scrollbar-thumb { background: color-mix(in srgb, var(--text-faint) 55%, transparent); }`(覆盖 `:650-654`,明暗两端都可见)。
- [ ] 派生变量进 `theme.css:15-22` 的 `[theme] {…}` 别名块(**不进 tokens.css**,否则破坏 TUI 生成器正则):`--accent-glow: color-mix(in srgb, var(--accent) 22%, transparent); --bg-glass: color-mix(in srgb, var(--bg-panel) 82%, transparent); --ok-bg: color-mix(in srgb, var(--ok) 12%, transparent); --warn-bg: color-mix(in srgb, var(--warn) 12%, transparent); --err-bg: color-mix(in srgb, var(--err) 12%, transparent); --info-bg: color-mix(in srgb, var(--info) 12%, transparent); --bg-raised: color-mix(in srgb, var(--bg-element), var(--text) 6%);`
- [ ] `App.jsx:23` 旁加 `useEffect(() => { document.documentElement.setAttribute("glass", state.glass ? "on" : "off"); }, [state.glass]);`;并在渲染层读 `location.search` 含 `smoke=1` 时强制 `"off"`(`gui/main.js:101-104` 锚点 `loadFile(builtIndex` 改为 `loadFile(builtIndex, { query: { smoke: "1" } })` 仅当 `DEEPSEEK_CODE_GUI_SMOKE === "1"`)。`@media (prefers-reduced-transparency: reduce)` 下 `[glass="on"] *` 的 `backdrop-filter: none`。

### P1.4 `.sn-*` 修复 + 通用模态壳

- [ ] `shell.css:692,697,706,713,714,715,719,723` 的未定义变量按映射替换:`--bg`→`--bg-base`、`--bg-1`→`--bg-panel`、`--bg-2`→`--bg-element`、`--bg-3`→`--bg-element`、`--fg-1`→`--text`、`--mut`→`--text-mut`、`--bdr`→`--border-strong`。(这是 v1.7.0 引入的既有 bug,顺带归零。)
- [ ] 末尾追加 `.modal-backdrop / .modal-card / .modal-close` 三个通用类(复制 `.sn-backdrop/.sn-card` 的布局,去掉 `--err` 描边),供后续复用;`SensitiveNoticeModal.jsx` **类名不动**、仍不允许点遮罩关闭。`.sn-backdrop` 的 `blur(2px)` → `blur(6px)`。

### P1.5 i18n

- [ ] `strings.js` zh/en 各加:`"settings.appearance.glass": "浮层玻璃态"` / `"Glass overlays"`、`"settings.appearance.glass.desc": "菜单、模态与会话输入框使用半透明模糊底;性能敏感时可关"` / `"Translucent blur on menus, modals and the chat composer; turn off on slow machines"`、`"theme.mode.toggle": "切换明暗"` / `"Toggle light/dark"`。

### P1 收口

- [ ] 五项准入;smoke 8 选择器与点击链原样通过;**人工目视**:8 张 smoke 截图 × `sumi / ash / snow / paper` 四套(`gui/__screenshots__` 不入库,只比对);`prefers-reduced-transparency` 下无模糊。
- [ ] 预期测试 ≈1098 → ≈1102(theme-hub +4)。
- [ ] **提交**:`feat(v1.8.0-p1): 壳层视觉层 — 主题中枢 + 标题栏令牌化 + composer 柔光 + 浮层玻璃 + .sn 修复`

---

# P2 · 卡片两段式 / 推理摘要 / 首页 / 设置›外观

**目标:** "原稿那种卡片"。前置 **v1.7.2 已合入**(assistant 回复入流;否则 2.1 的回复卡是空的)。

### P2.1 卡片两段式(`ChatView.jsx` + `shell.css` 追加)

- [ ] `ChatView.jsx:12-24` `Collapsible`:`.ev-h` 从 `<div onClick>` 改为 `<button type="button" className="ev-h" aria-expanded={!closed} onClick=…>`(a11y 欠账顺带修;类名不变)。
- [ ] CSS 追加:`.ev-h { font-family: var(--font-mono); font-size: var(--fs-12); display: flex; gap: 8px; align-items: center; width: 100%; text-align: left; }`;`.ev-b { background: var(--code-bg); border-top: 1px solid var(--border); }`;`.df .ln.add { background: var(--ok-bg); } .df .ln.del { background: var(--err-bg); }`(**不硬编码** `#10b981/#ef4444`——原稿在 5 个明主题上全不过 3:1);`.ev.ap { border-color: var(--warn); background: var(--warn-bg); } .ev.ap[data-decision="approve"] { background: var(--ok-bg); border-color: var(--ok); } .ev.ap[data-decision="deny"] { background: var(--err-bg); border-color: var(--err); }`。
- [ ] `ApprovalCard`(`:115-134`)根元素加 `data-decision={card.decision || undefined}`;`agent-cards.js:74-77`(锚点 `approval:resolved`)已回填 `decision`,若字段名不同以其为准。文案去掉 `(APPROVE)/(DENY)` 尾注(若有)。

### P2.2 工具卡耗时(真实数据,kernel 零改动)

- [ ] **写测试** `tests/unit/apps/event-contract.test.js` 追加:`describeEvent({ type:"tool:result", id:"c1", result:{ status:"ok", durationMs: 38 } }).fields.durationMs === 38`;缺失时 `=== null`。
- [ ] `src/apps/event-contract.js:68-71` `tool-result` 字段加 `durationMs: num(event.result?.durationMs)`(`src/tools/executor.js` 结果对象已带 `durationMs`,`:26,38,53,71,83`)。
- [ ] **写测试** `tests/unit/gui/agent-cards.test.js` 追加:tool:result 带 durationMs 时卡片 `durationMs === 38`。
- [ ] `agent-cards.js:56-58` 回填 `card.durationMs = f.durationMs`;`ToolCard`(`ChatView.jsx:56-73`)头部显示 `{card.status} · {card.durationMs} ms`(有值才显)。

### P2.3 推理摘要卡(展示契约门控;**正文不做**,网关不透传)

- [ ] **写测试** `event-contract.test.js` 追加:`describeEvent({ type:"model:response", purpose:"plan", model:"deepseek-v4-pro", usage:{ completion_tokens: 120, completion_tokens_details:{ reasoning_tokens: 80 } } })` → `kind==="thought"`、`quiet===true`(**保持 true**,所以 `:5-9`「malformed → other/quiet」与既有 NOISY 语义不变)、`fields.reasoningTokens===80`、`fields.purpose==="plan"`。
- [ ] `src/apps/event-contract.js:59`:`if (NOISY.has(type)) return d("other", …)` 前插一个分支:`if (type === "model:response") return d("thought", src, "info", true, { purpose: str(event.purpose), model: str(event.model), channel: str(event.channel), toolCallCount: num(event.tool_call_count), completionTokens: num(event.usage?.completion_tokens), reasoningTokens: num(event.usage?.completion_tokens_details?.reasoning_tokens) });`。**CLI 与 TUI 零变化**:CLI `render-events.js:3` 有自己的 `QUIET_EVENTS` 含 `model:response`,TUI `event-cards.js` 有自己的 `QUIET`——两端测试 `tests/unit/tui/event-cards.test.js`(8 条)与 CLI 相关测试**必须原样绿**作为证据。
- [ ] `agent-cards.js` 加分支:`type === "model:response"` 且 `(f.reasoningTokens > 0 || f.purpose)` → `cards.push({ kind:"thought", purpose:f.purpose, model:f.model, reasoningTokens:f.reasoningTokens, completionTokens:f.completionTokens })`;否则**不出卡**(这就是门控)。
- [ ] `ThoughtCard`(可放 `ChatView.jsx`):`Collapsible defaultClosed`,头「{t("ev.thought")} · {purpose} · {reasoningTokens} tokens」,体一行 `t("ev.thought.hidden")`。
- [ ] i18n:`"ev.thought": "推理摘要"` / `"Reasoning"`、`"ev.thought.hidden": "推理正文由网关隐藏,仅显示用量"` / `"Reasoning text is withheld by the gateway; only usage is shown"`、`"ev.tool.duration": "{n} ms"`。

### P2.4 数据新鲜度(`.cz-meta` 真实性)—— **已由 v1.7.2 任务 2 完成,本节勾掉即可**

> v1.7.2 已落地 `refreshLoadsFor(eventType)` 与 `useKernel` 接线(见 [v1.7.2 plan 任务 2](2026-09-17-v1.7.2-gui-defects.md))。P2 开工时确认 `tests/unit/gui/kernel-loads.test.js` 含「refreshLoadsFor re-pulls usage + checkpoints on turn end」即视为完成,不重复实现。

- [x] 由 v1.7.2 完成

### P2.5 首页 + 设置›外观

- [ ] `shell.css:657-660` `.hello-txt` 渐变改为 `linear-gradient(92deg, var(--accent) 10%, var(--info) 90%)`(原用 `--ok`;forest 退役后无 accent=ok 同色主题,但 info 更稳);`.q-card:hover` 加 `box-shadow: 0 8px 22px var(--accent-glow)`(覆盖 `:665-668`)。
- [ ] `gui/src/components/Settings/ThemeHub.jsx`(新):四段分组(`themesByTier()`),每段标题 `t("settings.appearance.<tier>")` + 说明行;卡片复用现行 `.th-card[theme]` 预览机制(`shell.css:640-643`,新主题自动有预览);选中卡下 `.th-detail`:5 色块(`bg-base/bg-panel/text/text-mut/accent`)+ hex + 点击复制 + **运行时实测对比度**(`getComputedStyle(document.documentElement)` 取值,复用 `contrastRatio`——从 `scripts/check-theme-contrast.mjs` import 会把脚本拉进 Vite 构建,**改为**把 `relLum/contrastRatio` 抽到 `gui/src/state/contrast.js` 并让脚本也 import 它);`glass` 一个 `Switch`(`Form.jsx:6`)。**无任何生理学文案。**
- [ ] `Appearance.jsx` 改为 `<ThemeHub …/>` 包一层,保留 `.f-group` 与底部 `settings.appearance.note`(`.s-nav` 顺序不动,smoke `.sn-item:nth-child(3)` 仍命中外观)。
- [ ] i18n:`"theme.detail.contrast": "正文对比度"` / `"Body contrast"`、`"theme.detail.source": "色板来源"` / `"Palette"`、`"theme.detail.copied": "已复制"` / `"Copied"`、四档说明 `"settings.appearance.dark.desc": "夜间 · OLED"` 等 8 条。

### P2 收口

- [ ] 五项准入;`event-contract.test.js`(12→14)、`agent-cards.test.js`(8→10)增量绿且既有条目不动;`kernel-loads.test.js` 已由 v1.7.2 增至 5 条,本层不动;**`tests/unit/tui/event-cards.test.js` 8 条与 CLI 测试原样绿**(证明 thought 分支对两端零影响);smoke 外观页截图仍出;人工:审批卡三态、diff 卡在 `paper/latte/snow/sand/lotus` 五套明主题下 add/del 行 ≥3:1(可把 `--ok/--err on --code-bg` 加进闸门脚本的"附加对",不计入 110)。
- [ ] 预期测试 ≈1102 → ≈1106(基线已含 v1.7.2 的 +4)。
- [ ] **提交**:`feat(v1.8.0-p2): 卡片两段式 + 推理摘要卡(门控)+ 工具耗时 + 数据新鲜度 + 设置›外观 ThemeHub`

---

# P3 · 收口

- [ ] **截图基线**:跑 smoke 两次(`sumi` 默认暗、`paper` 默认明,经偏好文件预置),把 `gui/__screenshots__/` 的 8 张各存一份到 `docs/prototypes/v1.8.0-ergo-restyle/screenshots/{sumi,paper}-*.png`(16 张,入库作定稿参照;`gui/__screenshots__` 仍被 `.gitignore:9` 忽略)。
- [ ] **死钩子清扫**(已核实 JSX/JS 零引用):`shell.css` 的 `.seg.click`、`.c-src.man`、`.mv-card .demo`;`strings.js:33/:173` 的 `settings.theme.night` / `settings.theme.day`(zh/en 各 2 键);**`gui/main.js:175`** 的 `await click(".rail-fn .fn-item:nth-child(1)");`(v1.7.2 遗留死 click——它在设置页执行,设置页无 Rail,静默失败;删掉该行,截图仍压在设置页上,这是既成基线,不改)。
- [ ] **版本四处**:`package.json:3`、`package-lock.json`(2 处)、`src/theme.js:2`、`gui/src/App.jsx:15` → `1.8.0`。
- [ ] **文档(按 docs/README 维护顺序)**:spec 状态行改「已实现」;本文各 checkbox 勾选;`project-overview.md` §15 主题集(10 套四段带)与偏好键(`lastDark/lastLight/glass`)、§16 TUI `/theme` 顺序;v1.4 spec §3.1 主题表加「**v1.8.0 更替**:退 6 加 6,见 v1.8.0 spec §3.3」附注、§3.2 表补六套新实测行;`CHANGELOG.md` `[Unreleased]` 移入 `## v1.8.0 — <日期> · 工学换肤`;`docs/README.md:166` 当前版本 → v1.8.0;索引里本 plan 登记。
- [ ] 五项准入 + e2e ×4(cli/tui/gui-build/gui-smoke)+ 手工:TUI `/theme` 连按 10 次顺序为 墨玄烬峡灰霜沙荷瓷宣。
- [ ] **提交**:`chore(v1.8.0): 收口 — 截图基线 + 版本同步 + 文档回写`,推 `feat/v1.8`。**打 tag `v1.8.0`、合入 main、推 main 与 tag 由维护者做**(执行者会话不允许推 main)。

---

## 附录 A · 六套新主题色值(✅ 已回填 2026-09-17)

> 派生方式:每套从官方色板起算,再由**两名独立复核者**各自用 WCAG 公式重算 11 对;六套 12 名复核者全部 `refuted=false`。另有第三方对其中五套独立复算 55/55 通过(lotus 后到,两名复核者数值一致)。**执行者在 P0.2 前须再跑一次 `node scripts/check-theme-contrast.mjs` 亲眼看到 110/110** —— 本附录是起点,脚本是裁判。
> 每套「偏离」列只记**闸门驱动**的改动;无槽位的派生值(hover/soft/inset 等)按现行 tokens.css 惯例取,已在色值行内体现。

### A.1 slate 玄 — Tokyo Night Storm(folke/tokyonight.nvim,storm 变体) · dark · bg L41

```css
/* 2 · 玄 slate — Tokyo Night Storm(folke/tokyonight.nvim;text-faint 取 dark5 而非 comment,见注) */
[theme="slate"] {
  --bg-base: #24283b; --bg-panel: #1f2335; --bg-element: #292e42; --bg-inset: #1b1e2d;
  --text: #c0caf5; --text-mut: #a9b1d6; --text-faint: #737aa2;
  --border: #3b4261; --border-strong: #414868;
  --accent: #7aa2f7; --accent-hover: #8eb0f8; --accent-soft: #2e3c64; --accent-text: #1d202f;
  --ok: #9ece6a; --warn: #e0af68; --err: #db4b4b; --info: #0db9d7;
  --code-bg: #1b1e2d;
}
```
实测:text/base 9.02 · text/panel 9.63 · mut 6.90 · faint 3.49 · accent 5.78 · accent-text/accent 6.41 · ok 7.97 · warn 7.28 · err 3.55 · info 6.42 · border-strong 1.71。
偏离:`text-faint` 官方 comment `#565f89` 仅 2.35 → 取板内 dark5 `#737aa2`(3.49);`accent-hover` 官方无 → blue 掺 15% 白。

### A.2 vesper 烬 — Vesper(raunofreiberg/vesper) · dark · bg L26

```css
/* 3 · 烬 vesper — Vesper(raunofreiberg/vesper;底 #101010→#1c1a17 提亮至 L26 以拉开 sumi;warn 改橙避免与 accent 同色) */
[theme="vesper"] {
  --bg-base: #1c1a17; --bg-panel: #221f1c; --bg-element: #282420; --bg-inset: #161412;
  --text: #ffffff; --text-mut: #a0a0a0; --text-faint: #707070;
  --border: #343434; --border-strong: #505050;
  --accent: #ffc799; --accent-hover: #ffcfa8; --accent-soft: #3e342b; --accent-text: #000000;
  --ok: #99ffe4; --warn: #ff7300; --err: #ff8080; --info: #99ccff;
  --code-bg: #161412;
}
```
实测:text/base 17.36 · text/panel 16.40 · mut 6.64 · faint 3.51 · accent 11.48 · accent-text/accent 13.88 · ok 14.70 · warn 6.37 · err 7.08 · info 10.28 · border-strong 2.65。
偏离:`bg-base` 官方 `#101010`(L16)→ `#1c1a17`(L26,档位下限);`bg-panel` 官方与画布同色 → `#221f1c` 拉层次;`warn` 官方 `#ffc799` **与 accent 同色**(会重演 forest 退役原因)→ 板内 `statusBar.debuggingBackground` 橙 `#ff7300`;`info` 官方无蓝 → 沿 ok 的粉彩明度转蓝 `#99ccff`。

### A.3 ash 灰 — Zenburn(jnurmine/Zenburn) · dim · bg L63

```css
/* 5 · 灰 ash — Zenburn(jnurmine/Zenburn;柔暗档,唯一偏离 bg-element 取 CursorLine 档) */
[theme="ash"] {
  --bg-base: #3f3f3f; --bg-panel: #383838; --bg-element: #434443; --bg-inset: #333333;
  --text: #dcdccc; --text-mut: #9fafaf; --text-faint: #989890;
  --border: #5f5f5f; --border-strong: #6f6f6f;
  --accent: #8cd0d3; --accent-hover: #93e0e3; --accent-soft: #366060; --accent-text: #2b2b2b;
  --ok: #7f9f7f; --warn: #dfaf8f; --err: #cc9393; --info: #94bff3;
  --code-bg: #303030;
}
```
实测:text/base 7.60 · text/panel 8.46 · mut 4.63 · faint 3.63 · accent 6.06 · accent-text/accent 8.14 · ok 3.59 · warn 5.35 · err 4.09 · info 5.52 · border-strong 2.10。
偏离:`bg-element` 官方 `#494949` 使 text 只有 6.5 → 取板内 CursorLine `#434443`(7.06)。**注意 text/base 7.60 余量最薄的一套**,后续任何提亮 bg 都要复核。

### A.4 snow 霜 — Nord Snow Storm(nordtheme/nord) · tint · bg L233

```css
/* 6 · 霜 snow — Nord Snow Storm(nordtheme/nord;浅底上 Frost/Aurora 语义色全部须压深) */
[theme="snow"] {
  --bg-base: #e5e9f0; --bg-panel: #eceff4; --bg-element: #d8dee9; --bg-inset: #d0d7e4;
  --text: #2e3440; --text-mut: #4c566a; --text-faint: #616e88;
  --border: #d0d7e4; --border-strong: #aebad1;
  --accent: #4c6c93; --accent-hover: #425d7f; --accent-soft: #c2d0e0; --accent-text: #eceff4;
  --ok: #6a8a4e; --warn: #a6781d; --err: #bf616a; --info: #3f8aa0;
  --code-bg: #d0d7e4;
}
```
实测:text/base 10.26 · text/panel 10.84 · mut 6.06 · faint 4.21 · accent 4.45 · accent-text/accent 4.70 · ok 3.22 · warn 3.24 · err 3.36 · info 3.22 · border-strong 1.61。
偏离(浅底上语义色几乎全压深,与 spec 预判一致):`accent` nord10 `#5e81ac`(3.50)→ `#4c6c93`;`ok` nord14 `#a3be8c`(1.67)→ `#6a8a4e`;`warn` nord13 `#ebcb8b`(1.28)→ `#a6781d`;`info` nord8 `#88c0d0`(1.64)→ `#3f8aa0`;`border-strong` nord4(1.11)→ `#aebad1`;`text-faint` 取 Nord 官方 VS Code 注释色 `#616e88`;`err` nord11 **原样**(3.36 已过)。**ok/warn/info 三对都只有 3.2x,余量薄。**

### A.5 sand 沙 — Gruvbox Light Soft(morhetz/gruvbox) · tint · bg L228

```css
/* 7 · 沙 sand — Gruvbox Light Soft(morhetz/gruvbox bg0_s;暖沙,与 snow 的冷雾成对) */
[theme="sand"] {
  --bg-base: #f2e5bc; --bg-panel: #ebdbb2; --bg-element: #fbf1c7; --bg-inset: #e0d0aa;
  --text: #3c3836; --text-mut: #665c54; --text-faint: #7c6f64;
  --border: #d5c4a1; --border-strong: #bdae93;
  --accent: #bb4506; --accent-hover: #af3a03; --accent-soft: #eacfa3; --accent-text: #fbf1c7;
  --ok: #79740e; --warn: #ad7013; --err: #9d0006; --info: #076678;
  --code-bg: #e0d0aa;
}
```
实测:text/base 9.23 · text/panel 8.45 · mut 5.18 · faint 3.87 · accent 4.21 · accent-text/accent 4.67 · ok 3.87 · warn 3.27 · err 6.86 · info 5.25 · border-strong 1.73。
偏离:`accent` 官方 orange `#d65d0e`(accent-text 压其上 3.41)→ 向 faded orange 插值 `#bb4506`;`warn` 官方 `#b57614` 恰好 3.0019 无余量 → `#ad7013`(3.27);`bg-inset`/`code-bg` 官方 bg2 `#d5c4a1` 使 text 6.76 → 取 bg1/bg2 中点 `#e0d0aa`(7.60)。

### A.6 lotus 荷 — Kanagawa Lotus(rebelot/kanagawa.nvim) · light · bg L239

```css
/* 8 · 荷 lotus — Kanagawa Lotus(rebelot/kanagawa.nvim;底提亮一档使整条 white 阶梯下移,正文取 lotusInk2) */
[theme="lotus"] {
  --bg-base: #f6f2d0; --bg-panel: #f2ecbc; --bg-element: #e5ddb0; --bg-inset: #dcd5ac;
  --text: #43436c; --text-mut: #545464; --text-faint: #8a8980;
  --border: #d5cea3; --border-strong: #a09cac;
  --accent: #4d699b; --accent-hover: #435b87; --accent-soft: #c7d7e0; --accent-text: #f6f2d0;
  --ok: #6f894e; --warn: #cc6d00; --err: #e82424; --info: #5a7785;
  --code-bg: #dcd5ac;
}
```
实测:text/base 8.21 · text/panel 7.75 · mut 6.55 · faint 3.10 · accent 4.86 · accent-text/accent 4.86 · ok 3.45 · warn 3.22 · err 3.94 · info 4.20 · border-strong 2.36。
偏离:**spec §3.3 写的 lotus 底色 `#f2ecde` 是错的**,Kanagawa 真实 lotusWhite3 是 `#f2ecbc`(L232,低于明档下限 236)→ 同色相提亮至 `#f6f2d0`(L239),整条阶梯下移:`bg-panel` 取 lotusWhite3、`bg-element` 取 lotusWhite2 `#e5ddb0`、`bg-inset`/`code-bg` 取 lotusWhite1 `#dcd5ac`;`text` 官方 fg lotusInk1 `#545464`(6.55)→ lotusInk2 `#43436c`(仍为官方色,lotusInk1 转任 text-mut);`warn` 官方 `#e98a00`(2.29)→ lotusOrange `#cc6d00`;`accent-text` 官方 `#dcd7ba`(3.80)→ `= bg-base`。**`text-faint` 3.10 余量仅 0.10,是全部 110 项中最薄的一对**。

### A.7 回填后的 spec 勘误

- spec §3.3 表中 lotus 起点 `#f2ecde ~236` 应改为 `#f2ecbc L232 → 采用 #f6f2d0 L239`(P3 文档回写时一并改)。
- 六套里 **余量 <0.3 的对**(后续改任何 bg 值前必看):ash text/base 7.60;snow ok/warn/info 3.22–3.24;sand warn 3.27;lotus text-faint 3.10、warn 3.22。

## 附录 B · 本 plan 引用的既有测试基线(2026-09-17)

| 文件 | 用例 | 本 plan 动作 |
|---|---|---|
| `tests/unit/gui/themes.test.js` | 3 | 改 1 断言 |
| `tests/unit/gui/workbench-state.test.js` | 24 | 改 2 断言 + 加 1 |
| `tests/unit/gui/kernel-host.test.js` | 18 | 改 2 个 deepEqual 期望 |
| `tests/unit/gui/gui-preferences-language.test.js` | 3 | 加 1 |
| `tests/unit/apps/tui-theme-palette.test.js` | — | 不动,必须仍绿 |
| `tests/unit/apps/event-contract.test.js` | 12 | 加 2 |
| `tests/unit/gui/agent-cards.test.js` | 8 | 加 2 |
| `tests/unit/gui/kernel-loads.test.js` | 5(v1.7.2 后) | 不动,必须仍绿 |
| `tests/unit/tui/event-cards.test.js` | 8 | 不动,必须仍绿 |
