# v1.7.2 GUI 缺陷补丁实施方案

> 完成状态以 [CHANGELOG](../../CHANGELOG.md) 为准。
> **状态:✅ 已实现(2026-09-19)。** 目标版本 **v1.7.2**(patch:只修现有缺陷,不改功能面);分支 `feat/v1.7`(v1.7.1 之后的补丁在同一小版本分支);**一个完整提交**,提交信息不带 `Co-Authored-By`。
> 来源:[post-V3 路线图 §2 E3/F2](../../specs/architecture/2026-09-17-post-v3-roadmap-design.md);**是 v1.8.0 P2 的硬前置**([v1.8.0 plan](2026-09-17-v1.8.0-ergo-restyle-plan.md) 的卡片重排依赖缺陷① 已修)。

**执行者须知:** 全部 `文件:行` 在 `main @ ccacbd3`(v1.7.1)核实;行号漂移时按每处给出的**内容锚点**找。严格 TDD:先写失败测试 → 跑到它失败 → 最小实现 → 跑绿。**不得触碰** `src/core src/deepseek src/tools src/edits src/sessions src/context src/security src/workspace`(收口用 `git diff --stat main..HEAD -- <八目录>` 证明为空)。本版只动 `gui/**` 与 `tests/**`。

---

## 全局约束

- 测试基线 **1085 全绿**(2026-09-17 实测),只增不减。
- 收口五项准入:① `npm test` ② `npm run check` ③ `cd gui && npm run build:renderer` ④ `DEEPSEEK_CODE_GUI_SMOKE=1 node --test tests/e2e/gui-smoke.test.js` 输出含 `GUI_SMOKE_READY` ⑤ 八目录 diff 为空。
- 三条缺陷各自独立、无先后依赖;任务 4(冒烟截图)依赖任务 3(模态样式修好后截图才有意义)。

---

## 三条缺陷的证据(2026-09-17 在 HEAD 复现)

| # | 缺陷 | 证据 | 用户看到什么 |
|---|---|---|---|
| ① | **agent 回复从不进消息流** | `workbench-state.js:81-96` `event_received` 分支只处理 `approval:requested` / `gui:sensitive_notice` / 错误类,**没有 `agent:final`**;`messages` 数组只在 `:78` `message_added` 写入,而 `App.jsx:80` 只在用户发送时 dispatch 它。`ChatView.jsx:207-219` 的 `.a-msg` 分支因此**永远渲染不出来** | 发一句话,看到自己的气泡和工具卡,**看不到 agent 的最终回复** |
| ② | **状态行数据首屏一次、永不刷新** | `kernel-loads.js:7-16` `buildInitialLoads` 只在 `useKernel.js:22-31` 挂载时跑一次;`onKernelEvent`(`:17-18`)只 `dispatch(eventToAction(e))`,回合结束后**不重拉** `getUsage` / `listCheckpoints` | `.cz-meta` 里 tokens / cache / 检查点数停在打开窗口那一刻 |
| ③ | **敏感文件模态引用 7 个不存在的 CSS 变量** | `shell.css:692,697,706,713-715,719,723` 用 `--bg / --bg-1 / --bg-2 / --bg-3 / --fg-1 / --mut / --bdr`,在 `tokens.css` 与 `theme.css` 中**定义处均为 0** | v1.7.0 的红色提醒模态在所有主题下**底色透明、文字无色**——安全提醒本身显示坏了 |
| ④ | v1.7.0 承诺的模态 Electron 冒烟截图**未兑现** | `gui/main.js:155-177` 截图链 8 景无 sensitive;`gui/__screenshots__/` 9-16 那批无此图 | 文档说有留档,实际没有 |

---

## 任务 1 · agent 回复入流(缺陷①)

**文件**:修改 `gui/src/state/workbench-state.js`;测试 `tests/unit/gui/workbench-state.test.js`(追加)

**接口**:`event_received` 收到 `agent:final` 时,`messages` 追加 `{ role: "assistant", text: <content> }`。字段名用 `text`——`ChatView.jsx:209,213` 渲染 `m.text || m.content`,`App.jsx:80` 用户消息也用 `text`,保持一致。

- [ ] **写失败测试**(追加到 `workbench-state.test.js` 末尾):

```js
test("agent:final appends an assistant message to the stream", () => {
  let s = state.createInitialState();
  s = state.applyWorkbenchAction(s, { type: "message_added", message: { role: "user", text: "hi" } });
  s = state.applyWorkbenchAction(s, { type: "event_received", event: { type: "agent:final", content: "done", status: "complete" } });
  assert.equal(s.messages.length, 2);
  assert.deepEqual(s.messages[1], { role: "assistant", text: "done" });
  // stopped(预算截停)同样入流——用户要看到"为什么停了"
  s = state.applyWorkbenchAction(s, { type: "event_received", event: { type: "agent:final", content: "Stopped: budget", status: "stopped" } });
  assert.equal(s.messages[2].text, "Stopped: budget");
});

test("agent:final with empty content does not append a blank bubble", () => {
  const s0 = state.createInitialState();
  const s = state.applyWorkbenchAction(s0, { type: "event_received", event: { type: "agent:final", content: "" } });
  assert.equal(s.messages.length, 0);
});
```

- [ ] **跑到失败**:`node --test tests/unit/gui/workbench-state.test.js` → 第一条 `messages.length` 实得 1,预期 2。
- [ ] **实现**:`workbench-state.js` `event_received` 分支(锚点 `if (event.type === "agent:error" || event.type === "session:rewind_conflict"`)**之前**插入:

```js
    // 缺陷①(v1.7.2):agent 的最终回复此前从不进消息流,.a-msg 分支永远渲染不出来。
    // 空 content 不追加(避免空气泡);stopped 也入流,让用户看到为什么停了。
    if (event.type === "agent:final" && typeof event.content === "string" && event.content.length > 0) {
      patch.messages = current.messages.concat([{ role: "assistant", text: event.content }]);
    }
```

- [ ] **跑绿**;既有 24 条(含 `:198` 的 `event_received agent:step` 不可变断言、`:215` 就地不修改断言)原样绿。
- [ ] **人工核对**(有 API Key 时):GUI 发一句「你好」,回复应以 `.a-msg` 气泡出现在工具卡之后。

## 任务 2 · 回合结束后刷新状态行数据(缺陷②)

**文件**:修改 `gui/src/hooks/kernel-loads.js`、`gui/src/hooks/useKernel.js`;测试 `tests/unit/gui/kernel-loads.test.js`(追加)

**接口**:新增纯函数 `refreshLoadsFor(eventType) → Array<{ call, toAction }>`(与 `buildInitialLoads` 同形状,便于 `useKernel` 用同一段循环执行)。

- [ ] **写失败测试**(追加):

```js
import { refreshLoadsFor } from "../../../gui/src/hooks/kernel-loads.js"; // 合并进文件顶部的 import

test("refreshLoadsFor re-pulls usage + checkpoints on turn end, nothing otherwise", () => {
  for (const type of ["agent:final", "agent:error", "turn:cancelled", "file:rollback_applied"]) {
    const calls = refreshLoadsFor(type).map((l) => l.call).sort();
    assert.deepEqual(calls, ["getUsage", "listCheckpoints"], type);
  }
  assert.deepEqual(refreshLoadsFor("model:request"), []);
  assert.deepEqual(refreshLoadsFor("agent:step"), []);
  assert.deepEqual(refreshLoadsFor(undefined), []);
  // toAction 与首屏加载同形状,reducer 无需新 action
  const usage = refreshLoadsFor("agent:final").find((l) => l.call === "getUsage");
  assert.equal(usage.toAction({ requests: 1 }).type, "usage_loaded");
});
```

- [ ] **跑到失败**:`refreshLoadsFor is not a function`。
- [ ] **实现** `kernel-loads.js`(追加在 `buildInitialLoads` 之后):

```js
// 回合边界后需要重拉的数据(缺陷②,v1.7.2):usage 与检查点在回合结束前不会变,
// 所以只在终态事件上刷新,不在 model:*/tool:* 上刷新(那会每回合打十几次 IPC)。
const TURN_END = new Set(["agent:final", "agent:error", "turn:cancelled", "file:rollback_applied"]);
export function refreshLoadsFor(eventType) {
  if (!TURN_END.has(eventType)) return [];
  return buildInitialLoads().filter((l) => l.call === "getUsage" || l.call === "listCheckpoints");
}
```

- [ ] `useKernel.js:17-18`(锚点 `api.onKernelEvent((e) => dispatch(eventToAction(e)))`)改为:

```js
      ? api.onKernelEvent((e) => {
          dispatch(eventToAction(e));
          for (const load of refreshLoadsFor(e && e.type)) {
            const fn = api[load.call];
            if (typeof fn !== "function") continue;
            fn().then((res) => { if (!cancelled) dispatch(load.toAction(res)); })
              .catch((err) => { if (!cancelled) dispatch(errorToAction(load.call, err)); });
          }
        })
```
  并把 `refreshLoadsFor` 加进 `:3` 的 import。`cancelled` 变量已在 `:16` 声明。

- [ ] **跑绿**;既有 4 条原样绿。
- [ ] **人工核对**:发一句话,回合结束后 `.cz-meta` 的 tokens 数应变化。

## 任务 3 · `.sn-*` 未定义变量(缺陷③)

**文件**:修改 `gui/src/styles/shell.css:689-723`;测试 `tests/unit/gui/css-vars.test.js`(新)

**映射**(按语义对位到 `tokens.css` 的 18 槽):

| 错误变量 | 改为 | 出现行 |
|---|---|---|
| `--bg` | `--bg-base` | 692 |
| `--bg-1` | `--bg-panel` | 697 |
| `--bg-2` | `--bg-element` | 719 |
| `--bg-3` | `--bg-element` | 723 |
| `--fg-1` | `--text` | 706, 719 |
| `--mut` | `--text-mut` | 713, 714, 715 |
| `--bdr` | `--border-strong` | 719 |

- [ ] **写失败测试** `tests/unit/gui/css-vars.test.js`(通用守卫,不止管 `.sn-*`——以后任何 `var(--x)` 引用未定义槽位都在单测层红):

```js
import test from "node:test";
import assert from "node:assert/strict";
import { readFileSync } from "node:fs";

const read = (p) => readFileSync(new URL(`../../../gui/src/styles/${p}`, import.meta.url), "utf8");

// 定义源:tokens.css 主题块 + :root,theme.css 的 :root 与 [theme] 别名块
function definedVars() {
  const defs = new Set();
  for (const css of [read("tokens.css"), read("theme.css")]) {
    for (const [, name] of css.matchAll(/(--[\w-]+)\s*:/g)) defs.add(name);
  }
  return defs;
}

test("every var(--x) used in shell.css / theme.css is defined in tokens.css or theme.css", () => {
  const defs = definedVars();
  const missing = [];
  for (const file of ["shell.css", "theme.css"]) {
    const css = read(file);
    const lines = css.split("\n");
    lines.forEach((line, i) => {
      for (const [, name] of line.matchAll(/var\((--[\w-]+)/g)) {
        if (!defs.has(name)) missing.push(`${file}:${i + 1} ${name}`);
      }
    });
  }
  assert.deepEqual(missing, [], `未定义的 CSS 变量:\n${missing.join("\n")}`);
});
```

- [ ] **跑到失败**:预期 `missing` 恰好列出上表 8 处(`shell.css:692 --bg` … `:723 --bg-3`)。**若列出的不止这 8 处,说明还有别的同类 bug,一并修、并记进 CHANGELOG**。
- [ ] **实现**:按映射表逐行替换 `shell.css:692-723`。
- [ ] **跑绿**。
- [ ] `package.json:12` `check` 脚本不需改(CSS 不走 `node --check`)。

## 任务 4 · 敏感模态冒烟截图(欠账④)

**文件**:修改 `gui/main.js:155-177`(截图链);无新测试(smoke 属门控 e2e,截图缺失只记 `SKIPPED` 不判红——沿用现行策略)

**难点**:模态由内核在主进程 push `gui:sensitive_notice` 事件触发,冒烟环境没有 API Key、不会真的走到编辑。**解法**:smoke 模式下由主进程直接向渲染层 push 一条构造好的事件——这正是 `kernel-host.js` 里 `pushEvent` 已有的通道,不需要新 IPC。

- [ ] `gui/main.js` 截图链(锚点 `await click(".settings-back");`)**之前**插入:

```js
          // v1.7.2:敏感文件提醒模态截图(#9.3)。冒烟无 API Key 走不到真实编辑,
          // 由主进程直接 push 一条构造事件,走与内核完全相同的 kernel:event 通道。
          await click(".rail-fn .fn-item:nth-child(1)");            // 回首页,模态压在其上
          win.webContents.send("kernel:event", {
            type: "gui:sensitive_notice",
            request_id: "smoke_sn_1",
            descriptor: {
              kind: "sensitive-file-write", severity: "danger", count: 1,
              paths: [{ path: ".env", reason: "secret-file", reasonKey: "sensitive.reason.secret" }],
              recordDir: ".deepseek-code/changes"
            }
          });
          await new Promise((r) => setTimeout(r, 350));
          await shoot("shell-sensitive-notice");
          await click(".sn-refuse");                                 // 关掉再截后续
```

- [ ] `.sn-refuse` 点击会经 `respondSensitive("smoke_sn_1", false)` → `sensitive:respond` IPC → `host.resolveSensitiveNotice`,未知 id 静默返回 `false`(v1.7.0 已有守卫,`kernel-host.js` `resolveSensitiveNotice`),**不会挂主进程**。
- [ ] **验收**:`DEEPSEEK_CODE_GUI_SMOKE=1 node --test tests/e2e/gui-smoke.test.js` 输出含 `GUI_SMOKE_READY` 且**不含** `SMOKE_SCREENSHOT_SKIPPED:shell-sensitive-notice`;`gui/__screenshots__/shell-sensitive-notice.png` 存在,肉眼确认:红色描边卡片、拒绝键在前、底色不透明(任务 3 修好的证据)。
- [ ] 把该 png 复制一份到 `docs/prototypes/v1.8.0-ergo-restyle/screenshots/v1.7.2-sensitive-notice.png` 入库(`gui/__screenshots__` 被 `.gitignore:9` 忽略,承诺的"留档"要落在仓库里)。

---

## 收口

- [ ] 五项准入全绿;预期测试 **1085 → 1089**(任务 1 +2、任务 2 +1、任务 3 +1)。
- [ ] **版本四处** → `1.7.2`:`package.json:3`、`package-lock.json`(2 处)、`src/theme.js:2`、`gui/src/App.jsx:15`。
- [ ] **CHANGELOG**:`[Unreleased]` 下移入新节:

```
## v1.7.2 — <日期> · GUI 三处现有缺陷 + 敏感模态截图欠账

> 纯 GUI 补丁,内核零改动;是 v1.8.0 换肤(P2 卡片重排)的硬前置。

- **agent 回复从不进消息流**:`workbench-state` 的 `event_received` 不处理 `agent:final`,`messages` 只在用户发送时写入,`ChatView` 的 `.a-msg` 分支自 v1.4.6 起从未渲染过。现 `agent:final` 追加 `{ role:"assistant", text }`(空 content 不追加;stopped 也入流)。
- **状态行数据首屏一次永不刷新**:`getUsage` / `listCheckpoints` 只在挂载时拉一次。现新增纯函数 `refreshLoadsFor(eventType)`,在 `agent:final / agent:error / turn:cancelled / file:rollback_applied` 后重拉两项(不在 model:*/tool:* 上刷,避免每回合十几次 IPC)。
- **敏感文件模态引用 7 个不存在的 CSS 变量**(v1.7.0 引入):`--bg/--bg-1/--bg-2/--bg-3/--fg-1/--mut/--bdr` 在任何样式表中均无定义,红色提醒模态在所有主题下底色透明、文字无色。按语义映射到 `tokens.css` 18 槽修正;新增 `css-vars.test.js` 通用守卫——`shell.css`/`theme.css` 里任何 `var(--x)` 引用未定义槽位即红。
- **v1.7.0 承诺的模态冒烟截图补上**:smoke 链新增 `shell-sensitive-notice` 一景(主进程 push 构造事件,走与内核相同的 `kernel:event` 通道),截图入库 `docs/prototypes/v1.8.0-ergo-restyle/screenshots/`。
- 版本同步为 `1.7.2`(四处);全量回归 **1089 单测** + `npm run check` + renderer build + gui-smoke 通过。
```

- [ ] `docs/README.md:166` 当前版本 → v1.7.2;`docs/README.md` plans/frontend 索引登记本文;`project-overview.md` §15 消息流一句补注(assistant 回复入流)。
- [ ] 本文状态改「已实现」。
- [ ] **提交 + 打带注解 tag `v1.7.2` + 推送 main 与 tag**(v1.7.1 至今仍未推送,一并推)。

---

## 与 v1.8.0 的衔接

本版合入后,v1.8.0 plan 的 P0 / P1 / P2 全部可开工。P2.1「卡片两段式」与 P2.2「工具卡耗时」都假设 `.a-msg` 已真实渲染;P2.4「数据新鲜度」在 v1.8.0 plan 里标了"可拆 v1.8.1"——**本版任务 2 已把它做了**,v1.8.0 P2.4 可直接勾掉(开 P2 时按此调整)。
